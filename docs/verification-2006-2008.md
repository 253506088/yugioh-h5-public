# 2006—2008 最终验证记录

本文件记录本批实际执行结果，执行跨 2026-09-13—14，最终提交检查于 2026-09-14 完成。Node.js v24.13.0，本机 Edge 与 Playwright；浏览器拦截外部请求，卡图缺失不影响运行。

## 自动化结果

- `npm.cmd test`：**2789 项通过，0 失败、0 跳过**。包含 1,257 项本批逐卡运行扫描、44 项专项规则、6 项素材解析测试，以及既有全部默认检查。
- 三年运行扫描：1257 张，0 异常；其中 476 张在通用夹具中留下自身效果发动记录。其余按常驻规则、已安排的状态与合法窗口检查；不是所有效果分支都被执行。
- 九套新预设：**72 场对战全部完成，0 异常**；与青眼、黑魔术师、HERO、守墓各交换先后手。
- 新预设赛事：**16 人、15 场全部完成**；决赛逐步回放与保存的最终快照一致。
- 浏览器合计：**61 组通过**，表内套件没有脚本错误。命令中重复执行的 Node 子套件不再计入额外测试数量。

| 浏览器检查 | 结果 | 本机报告 |
| --- | --- | --- |
| 原有离线／组卡／仪式 UI | 8 组通过 | `output/v4-browser-report.json` |
| 原有三语／连锁／在线补图 UI | 12 组通过 | `output/v4.1-browser-report.json` |
| 字号与分页 | 5 组通过 | `output/experience-browser/report.json` |
| 大字号／多语言／小屏布局 | 5 组通过 | `output/experience-layout-browser/report.json` |
| 连锁演出／响应／十首配乐 | 7 组通过 | `output/experience-response-browser/report.json` |
| 竞技场／导入导出／回放／恢复 | 10 组通过 | `output/tournament-browser/report.json` |
| AI 重复效果保留 | 4 组通过 | `output/ai-marginal-browser/report.json` |
| 2003—2005 年度回归 | 4 组通过 | `output/years-browser/report.json` |
| 2006—2008 年度及二重召唤 UI | 6 组通过 | `output/gx-browser/report.json` |

## 构建与资源

- 正式成品：根目录 `index.html`，52,042,511 字节，约 49.63 MiB。
- SHA-256：`0bec26c68180c4db67286a541a0fad6a8f61485637920312d17e0d4c2442b7ca`。原有两组主浏览器报告、新年度报告及上一年度回归报告均核对这一成品哈希。
- 3,695 张可组卡卡片；1999—2008 快照 3,571 条；26 套预设；本批九套各 40 张主卡。
- 本地中、英、日卡名与说明均为 3,695 张；缺失卡文和百科链接为 0。
- 内嵌 245 张卡图、11 张卡框、卡背及 10 首 BGM。完整构建与无卡图构建都成功。
- 对新增预设桌面日文页及手机英文图鉴截图进行了目视检查；布局、卡片与分页没有溢出视口。

## 暂存提交的隔离重建

使用 `git checkout-index` 将暂存文件单独导出到 `output/submission-gx-2026-09-13T15-59-34-997Z/`，不包含依赖目录、研究网页、下载缓存或原来的 `output/`。复用本机已安装的 Node 依赖运行时，完成：

1. 从提交的十年规范化快照重新导入 3,571 条身份。
2. 从提交的 CDB 离线同步 3,695 张卡的三语资料。
3. 从提交的源码和资源重新生成 HTML，含 3,695 张卡、26 套预设及 245 张卡图。
4. 拦截 HTTP 的浏览器成功启动该重建成品，0 脚本错误，0 待实现登记。

隔离目录不含忽略的音频下载文件，因此重建版本使用清单中的 10 个在线音乐 URL。正式提交的根目录 HTML 已内嵌 10 首音乐，两者口径分开记录。

暂存的正式 `index.html` 与通过浏览器验证的工作区成品 SHA-256 完全相同。来源重建报告保存在 `output/rollout-2006-2008/staged-source-smoke.json`。

## 实测范围

运行扫描使用真实引擎与有资源的夹具，但不证明每张卡的每个分支、全部历史禁限表、完整官方裁定或所有真实设备都已验证。规则适配范围见 [批次交接](../交接/2006—2008批次交接.md)，牌表为 [历史战术改编](decks-2006-2008.md)。

旧失败现场、研究网页、日志、截图、录像及隔离检出目录继续保留在 `output/`，由 `.gitignore` 排除。Git 中保留复现用源码、年度快照、三语资料、测试、说明及正式 HTML。
