# 游戏王卡池按年推进方案 · V4.1 修订

修订日期：2026-09-10。当前执行结果和操作命令见 [MANUAL.md](操作手册)，后续交接见 [HANDOFF.md](交接/1999~2001批次（首次）交接)。此前方案保存在 `docs/history/YEARLY-PLAN-before-v4.md`。

## 优先级

1. 卡片资料、可执行效果、召唤规则、组卡和存档。
2. 自动测试、AI 对局、界面验证、可复现构建和文档。
3. 可选本地卡图。由用户在方便时运行增量采集器，绝不阻塞前两项。

**缺图不阻塞任何正常构建。** 游戏优先使用本地内嵌卡图，缺图可使用备用卡面或用户开启的在线原版卡图。V4.1 根据用户后续要求恢复在线开关；手动采集、构建内嵌和运行时补图是独立流程。

## 起点与口径

现行 KONAMI OCG 起于 1999 年，首包 Vol.1 的发行日期为 1999-02-04。1998 年 Bandai 版和 2002 年起国际 TCG 不与本轮日本 OCG 日期混用。

每张卡保留稳定内部 ID、供应方 ID、首次 OCG 日期候选和原始说明。再版不复制出新的卡片逻辑，异画通过独立图片身份和哈希保存。

年度范围包括常规包、预组、限定产品、书籍 / 游戏附卡、活动赠卡等；只抓某一类卡包不能证明年度全集完整。Rush Duel、技能卡、动画自创卡不混入本路线。衍生物另计，不编入卡组。

参考入口：[KONAMI OCG](https://www.yugioh-card.com/japan/)、[Vol.1 资料](https://yugipedia.com/wiki/Vol.1)。本地 `firstOCGDate` 数据仍标注为供应方候选；不要把链接存在、抓取成功或全部导入当成官方全量核验。

## 首四年现状

| 年份 | 本地快照数量 | 游戏资料与效果登记 | 官方产品交叉核验 |
| --- | ---: | --- | --- |
| 1999 | 623 | 已接入本作 | 未完成 |
| 2000 | 337 | 已接入本作 | 未完成 |
| 2001 | 242 | 已接入本作 | 未完成 |
| 2002 | 293 | 已接入本作 | 未完成 |

合计 1,495 张，46 张沿用原有身份，新增 1,449 张；总卡池 1,653 张。补齐 16 对仪式召唤，保留原有所有召唤方式；2002 批次新增 2 对仪式（白龙骑士、天空骑士 希雅托）、10 只同盟怪兽、VWXYZ 接触融合与守墓 / 亚马逊系列，并新增「守墓 · 王家长眠之谷」预设。

当前 1,653 张可组卡卡片均已配齐中、英、日名称和说明；语言资料原件与来源哈希单独保留。V4.1 构建已内嵌全部 1,653 张本地插画（2002 批次图片由用户于 2026-09-10 增量采集补齐）；每次实际内嵌数量以构建报告为准。图片数量不代表历史首印已经人工核对，也不证明正式裁定实现完成。

## 每年按小批次推进

1. 准备一年范围，例如 `npm run cards:year -- 2003`。
2. 先执行 `collect:years -- --scope data/yearly/scope-2003-2003.json --no-images`，保留原始响应和异常项。
3. 核对新卡、再版、缺日期、错年、缺失素材和卡片身份；用 `locales:sync` 为新入库卡提取三语，快照缺少的新卡再更新文本来源。
4. 按包或约 20—50 张卡分批落实效果，通用模式复用处理器，特殊规则单独实现。
5. 补足所需规则钩子和有意义的反例测试；新类型不能被当成普通怪兽直接导入。
6. 验证无图构建、组卡、AI、三语切换、对局保存和恢复；更新逐卡及语言覆盖报告。
7. 构建单 HTML 并归档。用户可随后单独执行 `art:collect -- --years 2003`，再重新构建。

2026 年批次须按执行当日截断实际发行日期，不能把未来公告卡算进“至今”。目前范围生成器按整年工作，届时需要额外的截止日期校验。

## 分开验收的四个维度

| 维度 | 检查内容 |
| --- | --- |
| 资料 | 身份、类型、数值、三语文本、百科链接、来源快照和日期候选 |
| 实现 | 基础规则 / 效果处理器已连接，有合法行动与结算路径 |
| 验证 | 规则案例、反例、AI、保存恢复和浏览器检查 |
| 图片 | 原件身份、解码、哈希和可选人工图像复核 |

实现登记不等于穷尽裁定认证；图片齐全不等于效果已做；单一供应方数据不等于年度官方全集。每批报告分别记录这些事实。

## 交付与保留

持续保留 `src/`、`scripts/`、`tests/`、`data/`、`assets/` 和 `output/`。构建成品为根目录 `index.html`；无图构建另存 `output/no-art/index.html`。每次正式构建、测试、采集都保留时间归档。

任何尚未完成的新增效果都应保留 pending 状态，并在组卡校验中阻止其被当作无效果卡进入对局。本轮已有明确规则适配的卡片在卡片说明和交接文档中标注，后续按需要升级精度。
