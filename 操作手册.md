# 游戏王 · 决斗之境 V4.1 操作手册

更新日期：2026-09-10。工作目录示例：`D:\code\my\2\ygo`。

本手册是当前操作依据。V4.1 新增中英日切换、对应卡片百科、实时采图进度，并恢复可选的在线原版卡图。旧文档中“必须在线取图”或“运行时绝不允许在线卡图”的描述均不再适用：现在本地图片优先，在线补图由用户开关控制，卡图始终不是游玩的前提。

## 1. 项目是什么

这是一个致敬高桥和希《游戏王》的单人卡牌决斗游戏。玩家对战本地 AI，可以选择预设牌组，也可以在组卡工坊从公共卡池自建、保存、复制、导入和导出牌组。

当前收录 **1,653 张可组卡卡片、14 套预设**。本轮将本地 1999—2002 年数据快照中的 **1,495 张卡**接入游戏，其中 46 张沿用既有卡片身份，实际新增 1,449 张。衍生物另外管理，不计入可组卡数量。

| 年度切片 | 本地快照记录数 | 年份依据 |
| --- | ---: | --- |
| 1999 | 623 | 首次 OCG 日期候选 |
| 2000 | 337 | 首次 OCG 日期候选 |
| 2001 | 242 | 首次 OCG 日期候选 |
| 2002 | 293 | 首次 OCG 日期候选 |

按 KONAMI OCG 的发行口径，首年为 **1999 年**，首包是 **Vol.1，1999 年 2 月 4 日**。1998 年的 Bandai 版、2002 年起的国际 TCG 是不同口径。本项目没有把 TCG 日期当作 OCG 首发日期。

**这些数字表示已保存的供应方快照，不等于独立核验过的官方年度全集。** 数据保留 `dateVerification: provider_candidate`、`productCoverage: not_crosschecked`；卡包、附卡、赠卡等完整年度产品清单仍须交叉核对。

已经具有仪式、融合、同调、超量、灵摆、连接召唤，两个共享额外怪兽区、连接箭头、连锁、效果代价、反转效果、准备阶段、临时控制权、装备怪兽和特殊胜利等。本作采用自己的规则引擎，不使用官方服务器或外部决斗服务。

## 2. 语言、版本和依赖

| 部分 | 技术 / 版本 |
| --- | --- |
| 游戏界面 | HTML5、CSS、现代 JavaScript，原生 DOM |
| 卡片与效果 | JavaScript 数据及手写效果处理器，非运行时文字解释 |
| 构建 / 采集 | Node.js ES Modules（`.mjs`），Node.js **22.13 或以上** |
| 建议 Node.js | 24 LTS；本机验证使用 24.13.0，开发运行时另验证过 24.19.0 |
| 图片处理 | `sharp 0.35.4`，用于解码、校验及生成较小的 WebP |
| 浏览器验证 | `playwright 1.62.1` |
| 依赖锁定 | `package.json`、`package-lock.json` |
| 测试框架 | Node.js 自带 `node:test`、`assert`，以及 Playwright |
| 游戏存储 | 浏览器 `localStorage`；无需数据库 |
| 文本提取 | Node 自带 `node:sqlite`，只在同步时读取保留的 CDB 文件；无需安装数据库服务 |
| 显示语言 | 简体中文 `zh-CN`、英文 `en`、日文 `ja` |
| 音效 | 浏览器 Web Audio，本地合成 |

没有 React、Vue、TypeScript 或外部脚本 CDN。构建后的 HTML 不需要 Node.js、npm、sharp 或 Playwright。规则、三语文字和内嵌图片均可离线运行；在线补图和打开百科需要网络。

项目版本是 **4.1.0**；对局存档结构版本继续为 **3**，以兼容原有 V3 对局。恢复代码接受 V1、V2、V3 存档，并补足新增的可选字段。

### 2.1 切换显示语言

在页面右上角、弹窗右上角或「设置 → 显示语言」选择 `中文`、`English`、`日本語`。首次默认中文，之后记住你的选择。卡名、完整卡片说明、灵摆效果、属性与种族、主要界面、具体效果按钮和玩法指南随之切换。

语言切换不会重开对局；已选素材、攻击 / 守备表示、召唤区域、连锁响应、图鉴筛选和工坊草稿均保留。自己输入的卡组名称保持原样，不会被自动翻译。图鉴与工坊可用任意一种语言搜索，宣言卡名的搜索也支持三语名称和编号。

V4 的英文混杂来自直接显示供应方英文数据；并不是此前已有完整的多语言功能。现在当前 **1,653 张可组卡卡片均具有三语名称和说明**。翻译只用于显示；内部卡片 ID、种族 / 属性代码、效果定义及存档保持原有含义。

卡图是同一份插画，切换文字语言不重复下载图片。「查看在线卡图」和「查看卡片百科」打开来源站的页面，其页面语言由来源站提供。

## 3. 第一次安装与启动

先安装 Node.js 24 LTS。在项目目录打开终端：

```powershell
cd D:\code\my\2\ygo
node --version
npm.cmd --version
npm.cmd ci
```

`npm ci` 使用锁文件安装确定的依赖。Windows PowerShell 若拦截 `npm.ps1`，使用 `npm.cmd` 即可，无需改变系统执行策略。macOS / Linux 将下文 `npm.cmd` 换成 `npm`。

源码与生成数据已保留，正常使用只需构建：

```powershell
npm.cmd run build
```

产物是根目录 **`index.html`**。可以直接双击，在现代 Edge、Chrome 或 Firefox 中打开。

也可以用项目附带的本地服务器：

```powershell
npm.cmd start
```

访问 `http://127.0.0.1:4173/`。该服务器只在本机监听。结束时在对应终端按 `Ctrl+C`。

端口已被占用时，可换端口：

```powershell
$env:DUEL_PORT = '4174'
npm.cmd start
```

## 4. 如何构建单 HTML

### 常规构建

```powershell
npm.cmd run build
```

构建器将卡片数据、三语文本、规则、界面、样式、卡背和**已经存在的本地卡图**写入一个 HTML。原始图片、源码、年度及文本数据库快照、脚本和测试文件继续保留。

构建过程不会下载图片。某张图片缺失、索引损坏或图片校验失败时，该卡使用可读的备用卡面，构建仍可完成。图片提示保存在 `output/build-report.json` 的 `artworkWarnings` 中。

每次构建另存一份到：

```text
output/v4-builds/<构建时间>/index.html
output/v4-builds/<构建时间>/build-report.json
```

### 快速无图构建

```powershell
npm.cmd run build:no-art
```

产物为 `output/no-art/index.html`，不会替换根目录成品。该版本保留完整游戏逻辑、全部三语文字及卡背；V4.1 约 3 MB，精确大小以构建报告为准。

无图版本默认开启「在线原版卡图」，方便联网补图。已有的开关偏好会优先沿用；如需完全不发起网络请求，在设置中关闭它。关闭或断网时显示可读备用卡面，仍能正常决斗和组卡。

也可以指定项目内其他输出位置：

```powershell
node scripts/build.mjs --no-art --out output/demo/index.html
```

### 修改卡片资料后

```powershell
npm.cmd run cards:import
npm.cmd run locales:sync
npm.cmd run cards:coverage
npm.cmd test
npm.cmd run build
```

`cards:import` 从本地年度 JSON 生成 `src/early-cards.js`，不访问网络。**不要只手改生成文件**，否则下次导入会覆盖修改；修改导入器、源快照的经核验修订或对应效果模块。

## 5. 卡图：由你手动增量采集

卡片开发和测试不依赖卡图。批量图片由你手动采集、构建时内嵌。游戏中的可选在线补图只负责当前显示，不会把图片写回项目目录；若希望供日后离线构建使用，仍须运行以下增量采集脚本。

设置中保留两个独立开关：

- **显示卡图**：控制图片显示总开关，关闭时使用备用卡面。
- **在线原版卡图**：没有可用内嵌图片时，允许从 YGOPRODeck 加载。常规构建初始关闭，无图构建初始开启；用户已保存的偏好优先。

卡片详情的图片链接旁有 **查看卡片百科**。即使关闭卡图，这个链接仍可使用；当前每张可组卡卡片都已保存对应的具体卡片页面地址。

### 5.1 先看还有哪些图片缺失

```powershell
npm.cmd run art:plan
```

该命令**不联网、不修改卡图索引**，只检查现有缓存并保存预检报告。输出中的 `reused` 是可以复用的图片，`pending` 是缺失项；预检不会因为缺图返回错误码。

### 5.2 下载当前游戏卡池缺失的卡图

```powershell
npm.cmd run art:collect
```

默认只采集 `cropped` 插画，足够用于游戏卡面。脚本会：

1. 从当前卡池读取卡片身份，排除衍生物。
2. 优先使用已有供应方 ID；缺少 ID 时按准确英文卡名查询，并缓存身份匹配。
3. 读取图片索引并校验文件 SHA-256。
4. **复用有效缓存，只请求缺失或损坏的图片。**
5. 解码检查图片，保存原件、WebP 派生文件、来源及结果报告。

再次执行同一命令就是增量续跑，不需要删除目录，也不需要 `--resume`。

脚本一启动就会显示卡数、图片总数和缓存检查结果，随后显示下载、复用、失败和剩余待处理数量。网络请求较慢时，每 5 秒输出当前操作。多张缓存命中会合并进度行，避免刷屏；下载开始、保存成功、重试和失败都会输出。

日志示例格式（计数以实际运行结果为准）：

```text
开始卡图采集：1653 张卡，1653 张图片；增量下载。正在检查本地缓存…
缓存检查完成：预计可复用 1202 张，需要补充 451 张…
[1203/1653] 已下载 1 · 已复用 1202 · 待补 0 · 失败 0 · 剩余待处理 450 · 5s 已保存：青眼白龙 (cropped)
```

“剩余待处理”指本轮尚未处理的项目；“待补”和“失败”仍需之后补齐，不会被计作下载成功。收尾同时列出仍需补全的图片数。预检阶段的缓存数量只是估计，真正复用前仍会核验文件哈希。

实时日志同时写入本次归档的 `progress.log`，中途按 `Ctrl+C` 也保留已写入部分。`collect:years` 也使用相同的进度输出。需要安静输出时加 `--quiet`；如需将纯 JSON 交给其他程序，直接运行 Node 或使用 npm 的 `--silent`：

```powershell
node scripts/collect-game-art.mjs --plan --quiet
npm.cmd --silent run art:collect -- --years 2001 --quiet
```

### 5.3 只抓指定年份 / 指定卡片

```powershell
npm.cmd run art:collect -- --years 1999,2000,2001
npm.cmd run art:collect -- --years 2001
npm.cmd run art:collect -- --ids blue-eyes,early-64631466
```

`--years` 按游戏中已记录的 `releaseYear` 筛选；没有年份的原有现代卡不会混入旧年份。`--ids` 使用内部卡片 ID，可从覆盖报告或牌组导出的 JSON 中查到。

如果还希望保存完整官方卡面：

```powershell
npm.cmd run art:collect -- --years 1999,2000,2001 --variant both
```

`--variant` 可为 `cropped`、`full`、`both`。两种资源独立计数；以前下载过 cropped，再抓 both 只需补 full。根目录 HTML 默认内嵌适合展示的 cropped 派生图，完整原图保留在本地档案中。

### 5.4 只核验缓存、绝不联网

```powershell
npm.cmd run art:collect -- --offline
```

若仍缺图片，退出码为 `2`，报告会注明缺失项。它不影响游戏构建。只有确实需要重新获取已缓存图片时，才使用 `--force`；日常增量不需要这个参数。

### 5.5 图片放在哪里

```text
assets/official-archive/index.json       图片身份、文件路径、哈希和来源
assets/official-archive/objects/         原始图片，按内容 SHA-256 命名
assets/official-archive/derived/         采集器生成的展示图
assets/build-art/384-q82/                构建用的较小 WebP 缓存
data/providers/game-art-identities.json  已匹配过的英文名 / 供应方 ID
output/game-art/run-*/                  原始响应、报告、索引快照、实时 progress.log
output/game-art/latest-report.json       最近一次采集 / 预检结果
```

既有 `assets/official/<内部卡片ID>.jpg|png|webp` 也继续支持，适合手动指定某张图片；此目录的图片优先于档案索引。

### 5.6 采集完成后重新构建

```powershell
npm.cmd run build
```

采集器不会自行覆盖正在游玩的 HTML。重新构建并刷新页面后，新增缓存才会进入成品。

### 5.7 退出码和网络异常

| 退出码 | 含义 |
| --- | --- |
| `0` | 成功完成；或完成 `--plan` 预检；或完成单独元数据采集 |
| `1` | 参数或脚本层面的错误 |
| `2` | 采集未完整完成，查看报告中的缺失 / 失败项 |

脚本限制请求速度，并对 429 / 常见服务器错误重试。中断后保留已写入的图片和索引，再次运行即可续取。网络失败不代表卡片效果没有实现，也不应阻止后续内容开发。

## 6. 按年份继续推进

### 6.1 创建下一年采集范围

```powershell
npm.cmd run cards:year -- 2002
```

生成 `data/yearly/scope-2002-2002.json`。已有同名配置不会被覆盖。也可以一次准备连续两年：

```powershell
npm.cmd run cards:year -- 2002 2003
```

### 6.2 优先获取资料，不抓图片

```powershell
npm.cmd run collect:years -- --scope data/yearly/scope-2002-2002.json --no-images
```

原始响应、规范化目录、隔离项和图片任务清单全部保留。成功完成 `--no-images` 退出码为 `0`。

续跑同一范围时可复用目录快照：

```powershell
npm.cmd run collect:years -- --scope data/yearly/scope-2002-2002.json --resume --no-images
```

如果希望这个年度直接连图片一起处理，去掉 `--no-images`。该年度采集器也按同一个哈希档案增量复用图片。

### 6.3 使用已经下载的原始目录

```powershell
node scripts/collect-years.mjs --scope data/yearly/scope-1999-2001.json --catalog output/yearly-collection/run-2026-09-09T02-33-23-930Z/raw/catalog.json --offline --no-images
```

这是当前保留的真实三年快照路径。若使用其他快照，以相应 `manifest-*.json` 的 `catalogSnapshot` 为准。

手动下载的图片可以按以下目录导入：

```text
my-images/cropped/<供应方图片ID>.jpg
my-images/full/<供应方图片ID>.jpg
```

```powershell
node scripts/collect-years.mjs --resume --offline --image-dir my-images
```

### 6.4 从资料到真正可玩的卡

采集后**不会自动把新年份标成已经实现**。目前游戏导入器正式接入的年份是 1999—2002；2003 以后是下一开发批次。

后续开发顺序：

1. 核对新增卡、再版和日期冲突，保留原始来源。
2. 扩展导入器中的年份、类型映射和生成数据；复用稳定内部 ID。
3. 为新增卡编写通用模板或专用效果，补需要的规则能力。
4. 添加能区分正确 / 错误行为的规则案例，测试 AI 和中途存档。
5. 更新年份筛选、覆盖报告、文档并构建；图片可以随后单独补。

覆盖报告：

```powershell
npm.cmd run cards:coverage
```

输出 `output/early-import/effect-coverage.json`。其中“implemented / existing”表示已连接到本作的实现；**不是官方裁定认证或每一种组合的穷尽测试结果**。

## 7. 游戏操作

在「新的决斗」选择「守墓 · 王家长眠之谷」可以直接体验 2002 批次的新机制：王家长眠之谷封锁墓地、守墓长无视封锁、间谍与守卫反转展开。另有「仪式 · 混沌的降临」「融合 · 初代的羁绊」等 2002 前预设继续可用。2002 批次还带来同盟怪兽（装备 / 解除 / 代替破坏）、VWXYZ 接触融合、守护者装备召唤条件、卡通世界直攻和亚马逊系列。

仪式召唤：发动对应仪式魔法，结算时选择手牌仪式怪兽，再选择手牌 / 场上的解放素材，最后选择攻击或守备表示与主怪兽区。素材必须满足仪式魔法的等级要求，不能解放多余素材。仪式怪兽不能通常召唤，也不放入额外卡组。

组卡：在「组卡工坊」从空白或预设副本开始，按名称、效果、类型、系列或年份筛选。仪式怪兽自动进入主卡组。主卡组 40—60 张、额外 0—15 张、同名合计最多 3 张；不使用赛事禁限卡表。保存后可在新决斗中选择。草稿会自动保留，支持撤销、重做和 JSON 备份。

图鉴每页 24 张，工坊每页 30 张。可以选单年，或「截至 2000」等累计切片。宣言卡名等大候选列表提供文字搜索，避免同时加载上千张卡图。

| 快捷键 | 功能 |
| --- | --- |
| `Space` | 下一阶段 |
| `E` | 结束回合 |
| `1`—`9` | 选择手牌 |
| `Esc` | 关闭窗口 / 取消尚未提交的选择 |
| `M` | 音效 |
| `F` | 全屏 |

## 8. 验证和报告

```powershell
npm.cmd test
npm.cmd run test:v4-simulation
npm.cmd run build:no-art
npm.cmd run build
npm.cmd run test:browser
npm.cmd run report
```

`npm test` 同时运行旧版本回归、新规则、2002 全卡扫描（293 张卡逐张放置 / 发动并经历完整事件序列）、三语完整性及采集 / 图片 / 进度测试，合计 482 项。图片测试使用明确标记的合成数据，不访问真实图片服务。也可单独运行 `npm.cmd run test:2002-sweep`。

`test:browser` 先执行关闭在线卡图的离线流程，再执行 V4.1 语言与图片开关流程。在线分支使用模拟图片响应，验证加载、优先级、失败回退、关闭时取消以及重新打开。`test:i18n`、`test:localization-browser` 可单独执行；`test:ui-audit` 保存中英日主要界面的截图和未匹配文案，供人工检查。

浏览器测试优先使用 Windows 已安装的 Edge。其他系统可安装 Playwright Chromium：

```powershell
npx playwright install chromium
```

也可通过 `DUEL_BROWSER` 指定兼容 Chromium 浏览器的可执行文件路径。

| 报告 | 内容 |
| --- | --- |
| `output/v4-checks-report.json` | 规则和采集测试汇总 |
| `output/v4-checks/<时间>/results.tap` | 完整测试输出 |
| `output/v4-simulation-report.json` | 预设与年度混合牌组模拟结果 |
| `output/v4-browser-report.json` | 浏览器交互、离线请求计数及截图路径 |
| `output/v4.1-browser-report.json` | 三语、选择保留、百科、在线 / 内嵌图片和移动布局 |
| `output/localization/card-coverage.json` | 三语覆盖、编号别名、缺失文字或百科地址 |
| `output/build-report.json` | 最近构建、卡数、图片数量、体积和告警 |
| `output/early-import/catalog-report.json` | 三年数据输入哈希与去重数量 |
| `output/early-import/effect-coverage.json` | 逐卡实现状态和处理器 |
| `output/v4-delivery-report.json`、`output/v4-QA.md` | 最终成品哈希、验证汇总和交付范围 |

`npm run report` 会检查浏览器验证的 HTML 哈希是否与根目录成品一致；改动游戏代码或卡图并重建后，应重新运行浏览器验证再生成交付报告。`npm run art` 是保留的历史 SVG 绘图脚本，当前原版卡图采集使用 `art:collect`。

## 9. 目录与扩展入口

| 路径 | 用途 |
| --- | --- |
| `src/cards.js`、`expansion-cards.js`、`link-cards.js` | 原有卡片和预设 |
| `src/early-cards.js` | 三年快照的生成模块 |
| `src/early-decks.js` | 新增仪式、初代融合预设 |
| `src/advanced-engine.js` | 现代决斗引擎、连锁、区域与存档 |
| `src/early-engine.js`、`early-engine-extra.js` | 仪式及早期规则扩展 |
| `src/advanced-effects.js`、`effects-*.js` | 效果注册、输入、代价、结算、事件、AI |
| `src/game-v2.js` | 当前维护的 V4.1 界面；文件名保留兼容历史 |
| `src/workshop.js`、`card-view.js` | 组卡、筛选、卡片展示 |
| `src/artwork-hybrid.js` | 当前图片层：内嵌优先，可选在线补图 |
| `src/artwork-local.js`、`src/artwork.js` | 保留的两代图片层，不参与当前构建 |
| `src/card-locales.js` | 生成的三语卡片文本、身份和百科地址 |
| `src/i18n.js`、`i18n-data.js`、`i18n-effects.js`、`i18n-help.js` | 语言偏好、显示代理、界面 / 效果按钮 / 指南翻译 |
| `src/index.template.html`、`*.css` | 页面模板和样式 |
| `scripts/` | 构建、导入、报告、增量采集与迁移脚本 |
| `data/yearly/` | 年度配置、规范化资料和批次清单 |
| `data/locales/` | 三语文本数据库原件、来源哈希、生成 JSON |
| `assets/` | 原图、派生图、卡背和历史插画 |
| `tests/` | 自动验证代码 |
| `output/` | 历史构建、原始响应、审计、测试与截图；不自动清理 |

卡片效果通过显式注册和规则函数运行。例如早期“火花”的处理器调用 `e.damage(1-c.owner, 200, '效果')`；文字本身不会被当作程序执行。支持对象选择、代价、一次限制、后续选择与 AI 评分。新增资料不应仅把状态改为 implemented，而不提供实际处理逻辑。

## 10. 当前范围与常见问题

- 当前卡池的中英日卡名和说明已配齐，来源原文仍保留。社区文本库可能采用不同译名或勘误进度；这不是 1999 年旧版文本 / 旧裁定模式，也不表示所有语言的文本都与本作独立规则完全等同。
- 本作仍是独立规则实现，复杂时点、连续效果的全部相互关系和全部卡片组合没有穷尽验证。已知适配点及后续审计任务见 `HANDOFF.md`。
- “灵魂消灭”使用预先指定替代除外顺序；“诱饵人偶”的可发动陷阱通过后续连锁处理。这些适配在卡片实现说明中标注。
- `Amphibious Bugroth` 的素材 `Ground Attacker Bugroth` 已随 2002 批次入池并可正常融合；另一个素材 `Defender of the Sea` 仍不在快照中，保留名称约束，没有伪造数据。`Temple of the Kings` 的 `Mystical Beast of Serket` 支线已随 2002 批次可用。
- 打开 HTML 后缺图：可继续游玩，也可开启「在线原版卡图」。如希望将图片供以后构建使用，手动采集后再重新构建。
- 图片已下载却未出现：检查 `index.json`、图片是否通过校验，以及是否已重新生成根目录 HTML。只重启服务器不会触发构建。
- 双击 HTML 时浏览器限制存储：用 `npm start` 的同一地址游玩，并导出重要牌组备份。不同浏览器、端口或来源之间的存储不会自动共享。
- 修改卡片后页面没变化：编辑的是源码，运行的是构建产物；重新执行 `npm run build` 并刷新。

卡图是最低优先级的可选资源。开发、测试、构建和交付都不等待图片下载。

## 11. 按年度增量维护三语文本

当前文本来源是 [mycard/ygopro-database](https://github.com/mycard/ygopro-database) 的简中、英文、日文数据库及已保留的 YGOPRODeck 英文资料。原始 SQLite CDB 文件保存在 `data/locales/sources/<commit>/`，具体版本、路径、文件大小与 SHA-256 在 `data/locales/source-manifest.json`。

正常构建直接使用已生成的 `src/card-locales.js`，不需要查询翻译服务。新增年度数据并接入游戏后，先执行：

```powershell
npm.cmd run locales:sync
```

这是离线操作：从保留的数据库中提取**当前卡池**三语文本，生成 `data/locales/cards.json`、`src/card-locales.js` 和覆盖报告。它不要求卡图存在，也不下载卡图。百科地址优先来自年度原始响应及已存身份资料，使用实际地址而非猜测英文网址。

若下一批新卡还没有对应文字，或希望主动更新文本来源，再执行：

```powershell
npm.cmd run locales:update
```

更新命令将最新提交的三个数据库保存到新的 `<commit>` 目录，保留旧快照；仅在有缺失百科资料时补查询相应卡片的元数据。语言数据库上游是整库文件，更新时可能下载完整的新快照；生成进入 HTML 的内容始终只限当前卡池。**图片下载仍是另一条独立的增量流程**。

同步返回 `2` 表示仍缺文字、灵摆分段或百科地址，应查 `output/localization/card-coverage.json`，不能把缺失翻译当作已经完成。若数据库编号与图片供应方编号不同，工具仅在英文名、攻守、等级的唯一匹配成立时记录 `textId` 别名，不修改 `providerId` 或图片 ID。

当前一个已核验示例是 Barrel Dragon：供应方 / 图片 ID 为 `81480461`，文本数据库 ID 为 `81480460`。之后补新年时继续保留这类身份对应证据，不凭译名猜配图片或改动规则身份。
