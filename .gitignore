# ===== 依赖 =====
/node_modules/

# ===== PVP 私有运行数据与部署配置 =====
# .pvp-data 包含访客凭证哈希、完整双方构筑、未公开局面及对局结果，只留部署机器。
# 同时忽略其他目录中的 SQLite 运行数据库、WAL/SHM/回滚日志和常见备份。
# data/locales/sources/ 下已经提交的 .cdb 是离线语言来源，继续保留，不在此排除。
/.pvp-data/
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
*.sqlite.bak
# 真实域名、服务端参数或凭证可能写入 .env；只提交无秘密的 .env.example 模板。
/.env
/.env.*
!/.env.example

# ===== 构建与验证 =====
# 2026-09-22 AI 卡组导入：正式模块、别名 JSON、生成的 src/card-catalog.js、
# 名录构建脚本、自动化测试、使用文档及根目录 index.html 需要提交。
# output/ai-import/ 下的无图构建、浏览器截图、测试报告和真实接口探测结果
# 均为本地验证产物，由下方 /output/ 统一排除。测试报告不得包含 API 令牌。
# 真实 API 令牌只放浏览器本地存储或已忽略的 .env.local；不写入源码与样例。
# 浏览器调试配置、录制与下载文件仅留本机。
# 联网组卡扩展：output/ai-research/ 的检索响应、临时网页、真实模型试验、
# 截图和验证报告也由 /output/ 排除；只提交正式检索适配器、校验器、界面和测试。
# 2026-09-23 优化问题2：output/optimization-2/ 的问题复现、弹窗/BGM截图、
# 历史牌表原图、联网采集响应和测试报告仅留本机；需求文档及其截图沿用个人文件排除。
# tests/fixtures/frog-bilingual.txt 是无密钥回归夹具；data/deck-research/ 是可复核来源清单，需提交。
/playwright-report/
/test-results/
/.ai-import-local/
# 2009—2012：正式源码、14 年离线快照、12 套牌表、三语资料、测试代码、
# 交接和根目录 index.html 均提交。新增王牌压缩 WebP 与来源哈希也提交。
# output/chronicle-* 与 output/rollout-2009-2012/ 中的研究网页、草拟脚本、
# 中间构建、逐卡失败现场、对局录像、截图、检查报告和暂存隔离副本只留本机。
# 大图下载原件仍在 assets/duel/art/source/，不重复提交；已有音频缓存规则不变。
# 根目录 index.html 是现有单文件发布入口，继续跟踪并随功能更新提交。
# 它内嵌本地配乐与可用卡图；下载源文件不额外提交。
# 构建报告、版本存档、测试截图等全部产物
# 2026-09-15 PVP 交付：只提交 server/、src/、tests/ 正式源码、部署配置、
# package/lock 文件、说明文档和重新构建的根目录 index.html。
# output/pvp-* 中的测试数据库、浏览器截图/报告、TAP 日志、提交范围审计、
# 临时脚本、提交信息草稿及构建备份全部留在本机，不提交到远端。
# 2026-09-13：包含机器人赛事实战录像 JSON、压力报告、界面截图、无卡图构建
# 和临时可视检查脚本；提交正式源码、离线所需数据、测试、说明及根目录 index.html。
# 赛事 JSON 可能含个人自定义构筑，不把测试／导出的本地赛事打包提交。
# 机器人收益优化的推演诊断、实战报告、浏览器截图也保存在 output/，仅留本机。
# 2026-09-22 推演层：output/scratch/ 下的剖析脚本、镜像对局分片结果与基线日志同样只留本机；
# 正式提交 src/ai-planner.js、tests/ai-planner.test.cjs、docs/bot-planner.md 与重建的 index.html。
# 出牌克制／守备／胜负提示的专项日志、截图、推演与赛事压力报告同样放在 output/。
# 详细决斗轨迹的导出样本、规则诊断和按回合界面截图也只保存在 output/。
# 本轮提交保留正式源码、测试、说明及根目录 index.html；提交排除审计也放在 output/。
# 年份推进的扫描日志、实战录像、浏览器截图及无卡图副本也仅留本机。
# 年度源码与 data/ 内来源快照不忽略，保证离线导入与重建；本批已获用户验收及提交授权。
# 2006—2008 批次：断电恢复备份、研究网页、卡片扫描现场、机器人对局及
# 赛事录像、浏览器截图、验证报告和无卡图构建全部放在 output/，保留本机不提交。
# 必须提交：data/providers/ocg-2006-2008.json、data/yearly/、三语数据、
# data/decks-2006-2008.json、效果源码、测试、交接文档和正式 index.html。
# 2026-09-24 2014 批次：必须提交 data/providers/ocg-2014.json、data/yearly/2014/、
# scope/manifest-2014-2014.json、data/decks-2014.json、scripts/lib/card-pendulum.mjs、
# src/effects-2014*.js、tests/chronicle-2014*.test.cjs、三语资料、交接/2014批次交接.md 与 index.html。
# 只留本机（由下方 /output/ 排除）：output/scratch/ 的分批卡表、草稿脚本与检查日志，
# output/chronicle-sweep/ 与 output/chronicle-simulation/ 的逐卡现场和对局报告，
# output/localization/ 覆盖报告，output/no-art/ 无卡图构建，以及尚未交付的
# 全局规则模式草稿 output/rule-mode-draft/（未接入构建，不属于本次提交）。
/output/

# 本轮资源：提交卡背、11 张卡框、预设/连锁预览用的压缩 WebP 和来源清单。
# 原始大图、源音频及试验封面属于本机下载缓存，保留在磁盘但不入库。
/assets/audio/*.mp3
/assets/audio/*.m4a
/assets/audio/*.ogg
/assets/audio/*.wav
/assets/audio/*.flac
/assets/duel/art/source/
/assets/duel/frames/source/
/assets/duel/cover.jpg
/assets/duel/cover2.jpg

# 本机临时诊断脚本，不作为项目测试套件提交
/scripts/live-check.cjs
*.log
*.tmp
# 原子写入遇到 Windows 文件锁时保留的诊断临时文件，不是构建输入。
*.tmp-*
*.part

# ===== 批量卡图档案与压缩缓存（体积巨大，不入库） =====
# 官方卡图原始归档
/assets/official-archive/
# 构建时的卡图压缩缓存（构建时自动重新生成）
/assets/build-art/
# official/ 目录今后只保留说明文件，新放入的图片一律忽略
/assets/official/*
!/assets/official/README.md

# ===== SQLite 运行时文件（打开 .cdb 时自动产生，无保留价值） =====
*.cdb-shm
*.cdb-wal

# ===== 编辑器 / 操作系统 =====
/.idea/
/.vscode/
*.code-workspace
Thumbs.db
.DS_Store

# ===== 本地个人文件 =====
/.claude/settings.local.json
/github pages发布.md
/需求文档/
