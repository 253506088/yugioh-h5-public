# 卡片效果怎样执行，怎样继续扩充

当前已推进1999—2012年度快照。最新规则分册、素材与复制处理、适配边界及验证入口见 [2009—2012交接](交接/2009—2012批次交接.md)。

这是一个纯JavaScript的决斗引擎。游戏时不调用语言模型，也不解释卡片的中文效果文字。联网功能只负责原版卡图；规则、AI、效果和存档全部在浏览器本地运行。

## 当前结构

| 模块 | 职责 |
| --- | --- |
| src/cards.js、expansion-cards.js、link-cards.js | 卡名、数值、卡片类别、素材条件、系列标记、卡组与展示文字 |
| src/advanced-effects.js | 注册效果、检查时点、收集代价／目标、处理事件与效果选择 |
| src/effects-*.js | 每张卡的具体效果脚本；按系列组织 |
| src/advanced-engine.js | 对局状态、区域移动、连锁、召唤、战斗、使用次数、存档及AI合法行动 |
| src/link-rules.js | 连接箭头、区域坐标、互相连接、Extra Link、Link素材计值 |
| src/game-v2.js、card-view.js、workshop.js | 渲染与交互，把合法行动和待处理选择显示给玩家 |

名称中含v2的界面文件是沿用的维护入口，当前构建版本为V4.1。旧版完整备份与迁移中间文件保留在output目录。

位置查询的优化位于 `advanced-engine.js`：每个引擎的WeakMap只记录区域提示，读取时必须重新核对当前槽位和UID，并返回当前对象；超量素材须再次定位其父怪兽。它不进入存档，也不会被AI的浅层实例复制共享。持续效果查询只缓存静态卡名身份，不能跨状态缓存生效结果。扩容后的性能回归及测量口径见 [2009—2012验证](docs/verification-2009-2012.md)。

## 一个效果的组成

注册效果时可以声明以下部分：

| 字段／入口 | 用途 |
| --- | --- |
| zones、main、speed、damageStep | 卡片在哪个区域、哪个时机能够发动 |
| condition | 判断具体发动条件，例如必须有合法目标 |
| inputs | 先选哪些代价或目标，数量和素材约束是什么 |
| cost | 支付发动代价；与效果结算区分 |
| resolve | 效果结算时执行什么 |
| once | 同名卡、这张实体卡、整场决斗等使用限制 |
| summons、destroys | 声明包含特殊召唤或破坏，供鲁莎卡、星尘龙等响应判断使用 |
| E.on、E.trigger | 监听召唤、移动、攻击等事件，产生诱发效果 |
| E.op、queueChoice | 保存多步骤效果的后续操作；玩家选择后继续结算 |
| E.passive | 注册持续数值、保护、通常抽卡数量等被动效果 |
| aiScore、aiResponse、extraCandidateScore | AI如何选择行动、响应和目标 |

例如已有的强欲之壶核心逻辑就是：

~~~js
E.spell('pot-of-greed', {
  label: '抽2张卡',
  condition: (e, c) => e.state.players[c.owner].deck.length > 0,
  resolve: (e, c) => e.draw(c.owner, 2)
});
~~~

它不负责手动画界面、操作DOM或独立结算连锁。注册后，公共引擎会校验时点、将它加入连锁、逆序处理效果并清理卡片。牌组编辑器和对战界面使用同一份卡池和规则。

## 为什么珠泪需要专门处理

“被效果送墓”与“丢弃作为代价”“连接素材送墓”“超量素材移除”“结束阶段弃牌”不是同一件事。V3的区域移动事件记录原因和是否由效果造成，珠泪只监听符合条件的送墓事件。

珠泪的融合使用一个可序列化的素材配置：从手牌、场上、墓地选择素材；必须包含触发效果的那张墓地卡；核对它当前所在区域和实体版本；材料放回卡组底部。这些条件在发动和结算时分别检查。异次元乌鸦在连锁中先除外触发卡，后面的融合就不会继续执行。

选择、目标身份、区域、连锁和剩余步骤都保存在状态中，因此能够在等待选素材时保存和恢复。

## 扩充难度

| 新内容 | 主要工作与难度 |
| --- | --- |
| 已有操作的新组合，如检索、抽卡、破坏、复活 | 增加卡片数据，注册效果并验证条件；通常不改公共引擎 |
| 新系列，如珠泪、黑羽等 | 编写系列效果与组合，处理次数、代价、替代处理及AI；主要成本是交互测试 |
| 新底层机制，如Link、仪式、控制权变化的新规则 | 需要补充引擎、界面、存档与回归测试；工作量明显增加 |
| 大规模接近完整官方卡池 | 不能只导入JSON或卡图；应考虑成熟规则核心及其卡片脚本，或进一步完善当前裁定框架 |

当前适合按系列、有明确组合地扩充几十到几百张。并非每张新卡都同样容易：已有函数能够复用，但特殊裁定、同时事件和跨区域互动仍需要专门代码。旧卡的一部分持续效果和特殊判断仍写在引擎分支里；本次新增了被动效果注册入口与可配置融合素材方案，后续可以继续减少这些分支。

如果目标变成完整OCG／TCG卡池，可以评估将成熟规则核心通过WebAssembly带入HTML。那会是另一项引擎集成工作，涉及核心、脚本、资源体积与授权；本次没有更换为外部引擎。

## 添加下一套卡组的顺序

1. 在独立数据模块中添加卡片和预设，使用唯一内部ID；英文原名用于匹配卡图。
2. 用效果模块注册实际行为。持续效果通过被动入口；发动效果与代价分开；需要玩家选择的后续步骤通过操作队列保存。
3. 将新模块加入scripts/build.mjs的依赖顺序；CommonJS测试入口也要加载该模块。
4. 检查主卡组40—60、额外0—15、同名最多3张等构筑条件。
5. 验证代表性组合、非法目标、被响应后的结算、使用次数、保存恢复，以及与旧卡组的混合交互。
6. 编译单HTML，进行真实点击与自动对局检查。

## 当前规则范围

已实现收录卡池所用的融合、同调、超量、灵摆、连接、共享额外区、箭头、互相连接、Extra Link和相应连锁。Link从墓地复活可使用主怪兽区；从额外登场时才要求额外区或合法箭头。

这不是完整OCG／TCG裁判模拟器。已有仪式、同调、超量等规则；仍未接入赛事禁限卡表。部分卡片文字采用本作范围内的中文表述，例如交织绵羊只处理本卡池存在的融合、同调和超量分支。
