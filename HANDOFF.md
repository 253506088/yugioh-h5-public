# V4.1 项目交接

## 2026-09-24：2014 年度推进

2014 年 **613 个身份全部登记效果，没有 `pending`**；可组卡 6,275 → **6,871**，预设 43 → **46**（影灵衣、机壳、影依三套年度牌组）。前半段（年度快照、灵摆导入、影依／星骑士／龙星／影灵衣与三套牌组）来自此前未提交的工作区，后半段补齐其余 511 张卡，分 9 个效果分册（`src/effects-2014-*.js`），并新增 `effects-2014-final.js` 让「百眼龙」等复制宿主能复制 2014 年的 185 个主动怪兽效果。44 张卡采用本作近似处理，逐卡说明在图鉴中可见。

**继续推进 2015 年及以后的卡片前，先读 [2014 批次交接](交接/2014批次交接.md)**：第 5 节是本批真实踩过的 15 个坑（`onEntry` 不支持强制、延迟效果需 `anyOwner`、op 重名覆盖、`Infinity` 存档失效、`self()` 只找场上卡等），第 6 节说明卡片代码怎样写才能兼容以后的全局规则类玩法。

验证：全量检查 **6,493 项通过、0 失败**（新增 2014 专项 125 项，逐卡扫描扩展到 613 张）；年代模拟通过；2009—2014 年度模拟 **200 局、0 异常**，附带的 64 人赛事 63 场全部完成且决赛录像逐步重放一致；chronicle／yearly／gx／v4／requested-ui 浏览器回归通过；已重新构建根目录 `index.html`。`v4.1-browser` 中「牌堆弹窗切换语言后返回」一项在上一次提交的干净副本上同样失败，属于既有问题，与本批无关。

## 2026-09-23：优化问题 2 合并修复

见 [完整修复与验收说明](docs/optimization-2.md)：弹窗逐层返回且遮罩不关闭；双语完整蛙牌表离线识别为 40 主 + 15 额外；废2等黑话、多主题独立联网构筑和真实 2010 世界冠军 41/15/15 原牌表；每局 BGM 从头随机且不连续重复。来源维护见 `data/deck-research/README.md`，研究原件与报告全部留在忽略目录。

## 2026-09-22：根据描述联网组卡

按用户要求，先将 AI 导入及维护交接推送为 `1cd52aa`，再开发此扩展。「AI 导入 → 联网组卡」使用公开 YGOPRODeck 搜索，支持来源推荐和当前卡池内调整；本地生成逐卡来源、验证构筑并在必要时补正一次。来源读取失败不会用模型记忆替代。架构、网络、测试与限制见 [联网组卡说明](docs/ai-deck-research.md)。

## 2026-09-22：AI 卡组导入与后续年份数据库维护

AI 导入已实现：YDK／YDKe／JSON／清单离线识别，OpenAI 与 Anthropic 抽取文本/图片，服务器抓取网址，逐卡确认、多卡组、备注和工坊保存。使用和验证见 [AI 卡组导入](docs/ai-deck-import.md)。

**继续推进 2014 年及以后卡片前，先读 [AI 卡片名录与年度维护交接](交接/AI卡片名录与2014以后年度维护交接.md)**。该文区分当前 6,275 张卡池、三语资料与 14,981 行全库名录，说明固定 CDB 来源、更新/重建顺序、别画与密码、状态转换、年份文案、回归验收和 Git 提交范围。

## 2026-09-22：机器人推演层

用户要求"按你的理解优化机器人出牌，不做限制"。新增 `src/ai-planner.js`（`DuelAIPlanner`），说明见 [机器人推演层](docs/bot-planner.md)。

- **局面价值**：怪兽、最高攻、手牌、后场、LP、战斗威胁、下回合伤害、手牌上限与卡组余量的统一估值，双方对称；对方里侧怪兽按 500/1600 估。
- **主阶段推演**：前 4 个候选（含通常召唤的另一种表示）各在副本上执行、再走 2 步旧策略并打完战斗阶段，与"直接进战斗"比较；只有全部候选都更差时才不出牌。
- **战斗推演**：每次攻击先在副本结算，收益 ≤ 0 不宣言；对方盖卡按数量折算；同等结果先用弱怪；取胜攻击优先。
- **响应门**：对方连锁窗口的响应必须"发动后局面更好"；默认分响应（如陷阱封印）不再空发；作者时机与付费反击陷阱的原判定保留。
- **顺带修正**：波动加农炮改为起动效果（此前在对方连锁上无限互连）；推演器提速约 8 倍；`casual` 难度保持旧策略。

验证：镜像对局 168 局推演层胜率 60.1%（两局都输 5 对）；`npm.cmd test` 5,687 项通过；三套长模拟通过；已重新构建根目录 `index.html`。

## 2026-09-21：生效中的效果、手坑与反击陷阱判断、AI 组卡设计

对应《需求文档/优化需求1.md》。前三项已实现并重新构建根目录 `index.html`；第四项按要求只出设计。说明见 [生效中的效果、手坑与反击陷阱判断](docs/lingering-and-bot-judgment.md) 与 [第三方 AI 组卡接入设计](docs/ai-deck-import-design.md)。

- **生效中的效果**：双方信息栏 LP 右侧显示已结算、仍在生效的效果芯片（一时休战的伤害归零、强欲而谦虚之壶的不能特殊召唤、和睦、效果遮蒙者的无效等），标注来源卡与期限；场上被无效的怪兽带角标；决斗场顶部 `◎` 按钮打开双方完整列表。观战与普通对局都能看到双方的条目，PVP 由服务器投影公开列表。新增 `src/lingering.js`，引擎在每个连锁环结算前后比较标记并归因，回合开始清理，旧存档兼容。
- **手坑不召唤**：只在手牌发动、攻击力不超过 1,200 的怪兽不再被通常召唤或盖放；作为立即可用的同调／超量素材，或是自己场上无怪兽且只有盖放能避免致死时例外。
- **反击陷阱判断**：需要支付 LP 或额外卡片的响应，会推演"放弃"与"发动"两条分支比较局面；神之宣告不再为只能炸掉自己的大风暴付一半 LP，仍会保护有价值的后场、怪兽与阻止强力召唤。
- **顺带修正**：巨大化海啸不再弹回自己（青蛙牌组无限循环）、魔导书之师只复制能处理的魔导书（赛事报错）、机器人不再选择付不起代价的响应（欧尼斯特对假面英雄 暗法）。见 `tests/simulation-fixes.test.cjs`。
- **AI 组卡设计**：两种协议（OpenAI 兼容含 Responses／Chat Completions 形态、Anthropic Messages）、自定义 URL／令牌／模型、文本／图片／网址输入、模型只抽取"卡名×数量×分区"而卡片身份由本地三语索引与密码确定、逐卡状态、服务器代理与 SSRF 防护、分期与待确认取舍。

本轮验证：`npm.cmd test` **5,675 项通过、0 失败**（含新增 11 项持续效果检查、12 项机器人判断检查与 5 项模拟修复检查）；新增 `tests/lingering-browser.cjs` **8 组**通过；`test:browser`、`test:experience`、polish、AI marginal、tournament、PVP 浏览器回归以及年度／年代／64 人赛事模拟均通过（赛事两个种子共 126 场、0 异常）。报告与截图在 `output/lingering-browser/`。

## 2026-09-21：2013 年度推进

最新批次为 **6,275 张卡、41 套预设**；新增 2013 年 528 个供应方身份，528 个身份的逐卡效果全部登记，没有 `pending`。五套年度代表构筑：征龙（2013 世界冠军）、魔导书（世界亚军）、入魔（OCG 上位第 3，因八强只有两种体系而按人气规则替换季军同款）、炎星四轴（两份 OCG 上位榜第 4）与武神（该年首发系列）。源码、修复的跨年问题、复现与提交范围见 [2013 批次交接](交接/2013批次交接.md)。

本轮同时修正了若干影响旧年份的问题：导入器不再把带 `unless` 例外的「Cannot be Normal Summoned」当成绝对禁止；龙骑兵与入魔 超越骑士的主阶效果从诱发改成主阶发动；共享候选列表同时接受卡片对象与 uid；超量素材文本支持两种书写顺序。`tests/chronicle-2013.test.cjs` 新增 14 项专项检查。

## 2026-09-15：PVP 再战提醒与 Git 交付

赛后发起再战时，对方的房间和结算页显示「玩家昵称已准备，希望与你再战一局」，同时给出一次轻提示；战场顶部会标记「再战邀请」。同意后仍需双方确认卡组并准备，实际准备完成也会通知另一方。准备状态以服务器为准，刷新／重连可恢复，重复同步不重复提示；取消准备或对手离开后清除提示。

包含此前已完成的结算页直接离开房间、房间页退出按钮优化，以及完整单局 PVP 服务。浏览器回归补充了结算页收到邀请、具名提示、重复消息、语言切换、刷新恢复和取消准备的真实双端验证。

提交正式服务端与客户端源码、测试、Docker／Compose／Caddy 配置、`.env.example`、依赖清单和锁文件、说明文档及根目录 `index.html`。`.gitignore` 已逐项说明：私有 SQLite、运行日志、真实 `.env`、测试截图／报告、临时脚本和提交审计只留本机；既有离线语言 `.cdb` 来源继续跟踪。

本轮重新构建正式 HTML，**5,098 项全量检查及 13 组 PVP 浏览器检查通过**，无页面／服务器异常；已验证具名通知、结算页内展示、重复同步去重、刷新恢复、取消准备与新的独立对局。Compose 普通和 HTTPS 配置校验通过；容器运行的验证边界仍见 PVP 文档。

## 2026-09-14：单局 PVP 联机服务

新增独立的 BO1 服务器与首页「联机对战」入口。`npm start`／`npm run start:pvp` 现在启动 `server/index.mjs`；原静态服务为 `npm run start:static`。使用方法、配置、部署、协议及验证口径见 [PVP 文档](docs/pvp.md)。

- 公开／私密房间、六位房间码、邀请链接、快速匹配、双方准备、3／5／10 分钟思考时间，以及每回合恢复 20 秒。
- 复用完整 `DuelEngine`，由服务器执行动作与结算；浏览器使用展示接口，不运行对手 AI。素材选择仍由服务器逐次校验。
- 按双方分别生成公开视图和日志。隐藏完整构筑、暗区身份、原始卡片编号、随机数和另一方候选；卡片句柄随玩家／局面版本变化。
- SQLite WAL 事务在广播前保存；支持刷新、自动重连、服务重启、最近请求去重、同一凭证接管连接。每人每局重连额度累计 60 秒，避免反复断线无限暂停。
- 自然胜负、特殊胜利、超时、认输、断线和保护平局分别记录。双方同意再战后重新准备，生成独立 gameId；不引入赛事和积分系统。
- PVP 不覆盖离线存档或单机胜率。支持三语大厅、原战场和手机布局。Docker／Compose／Caddy 配置已提供；私有数据库与 `.env` 已加入忽略规则。

新增主要文件位于 `server/`、`src/pvp-*`、`src/i18n-pvp.js` 与 `tests/pvp*`。`scripts/build.mjs` 已注册全部客户端模块，`npm test` 已注册 PVP 单元和网络检查。浏览器截图及报告保存在 `output/pvp-browser/`，全量结果以 `output/v4-checks-report.json` 为准。

服务采用单进程房间模型，访客凭证不等同于跨设备账号；保持项目现有自由构筑与裁定边界。不得让多个 Node 实例同时使用同一房间数据库。

本次验证：**5,098 项全量检查、50 组浏览器检查通过**，其中新增 46 项 PVP 单元／网络检查和 13 组 PVP 浏览器检查。原静态预览服务器也已拒绝发布隐藏文件和 SQLite 数据库。Compose 普通／HTTPS 配置校验通过，容器运行与公网证书签发未实测；详情见 PVP 文档末尾。

## 2026-09-14：熔岩魔神与扣血归属修复

已修复熔岩魔神扣错召唤者生命值，以及连锁能量、诅咒纸符等同类归属问题；补齐相关离场伤害，兼容旧对局存档。根目录 `index.html` 已重新构建。新增 117 项专项用例，全量 5052 项测试和正式 HTML 浏览器回归均通过。卡片范围、规则区别与复查命令见 [扣血与代价归属修复记录](docs/controller-costs-fix.md)。

## 2026-09-14：2009—2012 年度推进

最新批次为 **5,754 张卡、38 套预设**；新增2,059个身份，复用34个身份，四年来源共2,093张，每年三套代表构筑。支持年度预设筛选、真实超量素材与手牌同调、遗式仪式、甲虫装机／水精鳞等组合及三语资料。源码、适配边界、复现与提交范围见 [2009—2012 批次交接](交接/2009—2012批次交接.md)，[牌组说明](docs/decks-2009-2012.md) 和 [最终验证](docs/verification-2009-2012.md)。以下均保留为旧批次记录。

## 2026-09-13：2006—2008 年度推进

最新批次为 3,695 张卡、26 套预设，新增九套年度代表构筑。源码、验证、来源及 Git 交付范围见 [2006—2008 批次交接](交接/2006—2008批次交接.md)；牌表依据见 [年度卡组说明](docs/decks-2006-2008.md)。下文的数量与发布状态保留为历史记录。


## 2026-09-13：机器人死斗竞技场

本轮把两机器人观战扩展为独立的淘汰赛系统，成品仍为根目录 `index.html`。使用说明见 [死斗竞技场](docs/tournament.md)。首页和新决斗模式选择器均有入口，`#arena` 可直接进入。

- 默认 16 人，可配置 2–64 人；随机或逐席指定本地预设／已保存自定义卡组，允许重复并自动编号，支持打乱签位与开赛前预览。
- 单败淘汰，非 2 的幂人数自动轮空；双向晋级图、全部场次、选手榜、冠军与多路直播台实时更新。
- 串行或同时 2／4／8 场，沉浸／快速／极速可随时切换。每场独立随机种子，降低并行数量时保留其余场次局面。
- 开赛时冻结参赛构筑。`DuelEngine` 增加可选 `deckSpecs` 构造参数，既有调用保持原行为；删除或编辑自定义卡组后，赛事仍使用报名时的构筑。
- 录像包含实际起始状态、操作及其随机状态、最终状态。支持拖动、回合跳转、逐步、0.5–8 倍速、切换场次／重赛局、返回直播，复用原棋盘但不写入普通对局存档。
- 独立 IndexedDB 档案、刷新后暂停恢复、赛事 JSON 导入／导出。存储失败会明确提示，并在当前页面内保留未保存档案，防止新建赛事时丢失它。
- 平局最多重赛 2 次；160 回合／2,400 步保护按 LP、累计伤害、种子抽签裁定，界面区分裁定与正常胜负。AI 异常保留录像并允许重赛，不自动判胜。

新增源码为 `src/tournament.js`、`src/tournament-storage.js`、`src/tournament-ui.js`、`src/tournament.css` 和 `src/i18n-tournament.js`；构建脚本已全部内嵌。手机竞技场隐藏普通决斗操作，直播控制条保留原 DOM，避免持续刷新中丢失滑杆或下拉框操作。

### 本轮验证

| 检查 | 结果 |
| --- | --- |
| `npm.cmd test` | **507 项通过，0 失败／跳过**；含新增 12 项赛事核心检查 |
| `npm.cmd run test:browser` | 原有 **20 组**浏览器检查通过 |
| `npm.cmd run test:experience` | 原有 **17 组**体验浏览器检查通过；12 项规则检查已计入 507 |
| `node tests/tournament-browser.cjs` | 新增 **10 组**通过，包括 14 套预设＋2 名自定义机器人完整 16 人赛事、真实滑杆点击、录像、刷新、导入导出、移动屏幕和存储失败恢复 |
| `node tests/tournament-simulation.cjs` | 两届 64 人赛事，**126 场全部完成、0 异常、0 保护裁定**；共 13,429 步，最高 29 回合，决赛逐步重放与最终状态一致 |
| 常规／无卡图构建 | 均成功；常规成品保留 245 张内嵌卡图和十首原声配乐 |

赛事单元测试也比较了相同阵容与种子在串行／并行下的全部实际操作和晋级结果，并验证中途存档、轮空、平局、异常、卡组快照及往返跳转。规则测试使用真实引擎，平局／裁定／异常保护使用明确的受控场景。

本轮合计 **507 项 Node 检查 + 47 组浏览器检查**，压力赛事另列，不混入这两个数字。报告和截图保存在 `output/tournament-browser/`、`output/tournament-simulation/`、`output/tournament-review/` 及原有验证输出目录。`npm.cmd run test:tournament` 可运行赛事核心与浏览器检查。

用户随后要求按顺序完成竞技场、机器人连锁优化和 2003—2005 年卡片。竞技场与连锁优化分别提交远端，年份推进暂不提交；执行结果和提交记录集中维护在 [20260913工作汇报.md](20260913工作汇报.md)。以下 2026-09-12 的发布记录属于此前批次。

## 2026-09-12：决斗体验升级、测试方法与提交资源

本节记录此前的决斗体验升级。当前卡池为 **1,653 张、14 套预设**，支持三语；字号、分页、主界面、一屏战斗、BGM、卡背卡框、连锁演出、响应模式和回合数的使用方法见 [决斗体验升级说明](docs/experience-upgrade.md)。

本次用户已明确要求更新交接文档、构建并提交到 Git 远端。文末保留的 2026-09-09 数量、图片采集安排和“未授权提交”表述是旧批次记录；本次提交目标为当前分支的 `origin/main`。

### 验证环境与结果口径

在 Windows / PowerShell、Node.js **v24.13.0**、Playwright **1.62.1** 和本机 Microsoft Edge **152.0.4191.66** 下验证。浏览器以 headless 模式运行，使用真实 DOM、真实规则引擎和真实媒体解码器。

| 入口 | 验证结果 | 计数说明 |
| --- | --- | --- |
| `npm.cmd test` | **495 项通过，0 失败、0 跳过** | 包含基础／扩展规则、回归、2002 扫描、采集工具、三语完整性和 12 项体验单元测试 |
| `npm.cmd run test:browser` | **20 组浏览器检查通过** | `v4-browser.cjs` 8 组，`v4.1-browser.cjs` 12 组 |
| `npm.cmd run test:experience` | **17 组浏览器检查通过**，另运行 12 项体验单元测试 | 界面 5 组、布局 5 组、响应与配乐 7 组；12 项单元测试已包含在上面的 495 项中，不重复计数 |
| 完整构建及无卡图构建 | 均成功 | 正式入口为根目录 `index.html`；`output/no-art/index.html` 仅作无卡图验证 |

合计是 **495 项 Node 自动化检查 + 37 组浏览器检查**。本次没有重新运行旧的随机模拟脚本，也没有把历史模拟结果计入这个数字。

### 连锁与响应是怎样验证的

规则测试见 [tests/experience.test.cjs](tests/experience.test.cjs)。测试用固定 seed 创建 `DuelEngine`，将明确的卡片安排到手牌、场上和墓地，通过实际 `act()` 执行发动、响应和放弃，检查引擎事件、卡片位置、LP、抽卡数量、连锁历史和存档恢复结果。没有通过修改展示文案来模拟结算。

- **双方三环连锁**：我方强欲之壶为 1、对方魔法干扰为 2、我方盗贼的七道具为 3；断言发动归属与编号、结算顺序 `3 → 2 → 1`、2 被 3 无效、1 仍抽两张、七道具支付 1000 LP。
- **丢失目标**：死者苏生选墓地青眼白龙，对方连锁“消失”将其除外；断言 1 的状态为目标丢失、原因指向 2，且没有把这次处理写成效果被无效。
- **来源离场的区别**：普通魔法被旋风破坏后仍可结算；需要保持在场的活死人的呼声被旋风破坏后记为“未能适用”。这两类情况不混写为无效。
- **因果归属**：新加入的王宫的敕命、原先已经生效的持续无效、上位连锁支付代价导致目标离场，分别检查归属；探测持续无效时不能提前破坏发动卡。
- **中途存档**：在响应尚未完成时 snapshot / restore，随后继续处理，确认原有编号、目标和归因仍然一致。
- **响应模式**：AUTO 略过泛用陷阱对普通盖放、开放时点等的询问，保留对方发动、召唤和攻击；ON 显示合法窗口；OFF 不跳过强制处理、素材和已经发动效果的选择。

浏览器测试 [tests/experience-response-browser.cjs](tests/experience-response-browser.cjs) 使用受控局面进入同一套正式 UI，再通过真实按钮进行选择与确认。它会先选择魔法筒，收起面板查看战局，打开卡片详情、返回、按 Esc，再恢复响应；过程中逐次比较完整引擎快照，确认机会没有被放弃，原选择仍保留。另实际触发三环连锁，检查演出中的 1／2／3、双方归属、无效提示和可回放历史；也检查目标丢失的回放文案。

### 界面、字号、分页与小屏是怎样验证的

[tests/experience-browser.cjs](tests/experience-browser.cjs) 验证首页继续同一局、字号调整、图鉴与工坊分页。分页检查包含切换到每页 48 张、输入越界页码、空搜索结果、首尾页禁用状态，并比较滚动卡片前后分页栏的纵坐标，确认底栏不跟随卡片滚走。工坊操作同时比较草稿，确保布局操作没有修改构筑。

一屏战斗的常规窗口是：**1440×900、1366×768、1920×1080、2560×1440、1024×768、390×844、390×670、844×390**。读取双方信息栏、四排主区域、额外区域、阶段条和手牌的 `getBoundingClientRect()`，确认这些区域完整落在视口内，文档的滚动宽高没有超过窗口。

[tests/experience-layout-browser.cjs](tests/experience-layout-browser.cjs) 补充了以下边界：

- **150% 字号 × 中／英／日三语 × 7 种窗口**：320×568、390×670、768×1024、1024×768、1280×720、844×390、640×360，共 21 种组合。
- 除检查视口边界，还检查卡区留在场地内部、侧栏没有覆盖战场、标题和工具没有重叠、全部阶段按钮可见，以及配乐控件没有盖住手牌。
- 放入 **20 张手牌**，确认只产生手牌区域内部滚动；滚到最后并点击最后一张，确认可以查看、场地仍完整、规则快照没有被查看操作改变。
- 在短屏工坊展开筛选、选择 2001 年、收起筛选、修改每页张数并滚动卡片，检查结果数、卡片区域高度和分页／页脚的可见性。
- 检查通常、效果、融合、同调、超量、灵摆、仪式、Link、魔法、陷阱的预览框；切换全图样式后核对图片实际覆盖范围。
- 设置字号 130%、图鉴每页 96 张、工坊每页 48 张后刷新，确认偏好和对局保持，首页可以继续原对局。

截图使用关闭有限 CSS 动画后的稳定画面；布局测量等待窗口样式与弹窗动画完成，避免把打开动画的中间帧当作最终布局。另对首页、桌面／手机战场、工坊、响应窗口和连锁演出做了截图视觉复核。

### 十首配乐是怎样验证的

- 来源、文件大小和 SHA-256 在 [assets/audio/manifest.json](assets/audio/manifest.json)。`audio:collect` 会核对本地哈希、验证媒体格式并复用已经下载的文件。
- 在浏览器中逐首创建 `Audio`，加载构建内嵌的真实音频，等待 `loadedmetadata`，断言十首都可解码，时长在 45—600 秒之间。
- 使用用户交互解锁播放，等待实际播放状态，再打开首页、图鉴、工坊、指南、返回战斗；检查场景、曲名、播放和恢复行为。
- 战斗单元测试连续取 120 次曲目，断言每轮六首不重播、相邻轮次也不连续重复；浏览器再实际连续切换 12 首进行同类检查。
- 单独关闭音效，确认 BGM 仍播放；关闭／恢复 BGM，确认没有改变音效开关。

自动化浏览器使用静音启动参数，验证的是解码与播放器行为，不是人工听音鉴定。浏览器测试拦截外部 HTTP 请求；“在线卡图”分支使用合成图片响应，覆盖加载成功、失败、重试、关闭开关后的迟到响应及内嵌图片优先。因此这些检查不保证外部音源／图片服务将来一直在线。

### 原有功能的回归范围

[tests/v4-browser.cjs](tests/v4-browser.cjs) 和 [tests/v4.1-browser.cjs](tests/v4.1-browser.cjs) 继续验证：无卡图离线启动、年度筛选、仪式召唤与素材合法性、保存待处理选择、可搜索的大候选宣言、工坊复制／导入／导出／保存／出战、机器人观战和猜拳、三语切换、选中素材／表示／区域／响应的保留、用户自定义名称不被翻译、牌堆／额外素材／结果窗口、百科链接和可选在线补图。

这次更新了旧测试中的导航入口和页码断言：从主界面进入功能，直接读取可编辑页码输入框的值。没有删掉失败的业务断言来获得通过。

### 从检出源码复现测试

在项目根目录执行；已安装依赖时可以跳过第一步。源音频未入 Git，完整音频浏览器测试会拦截在线请求，因此首次复现须先取得本地音频。

```powershell
npm.cmd ci
npm.cmd run audio:collect
npm.cmd run build:no-art
npm.cmd run build
npm.cmd test
npm.cmd run test:browser
npm.cmd run test:experience
```

保留仓库中的压缩 WebP 即可重建预设／连锁预览卡图；需要重新获取时执行 `npm.cmd run art:experience`。macOS / Linux 将 `npm.cmd` 替换成 `npm`。浏览器路径可通过 `DUEL_BROWSER` 指定；当前 Windows 默认使用本机 Edge。

本机证据位置：

| 位置 | 内容 |
| --- | --- |
| `output/v4-checks-report.json`、`output/v4-checks/<时间>/results.tap` | 495 项 Node 测试的汇总与逐项结果 |
| `output/v4-browser-report.json`、`output/v4.1-browser-report.json` | 20 组原有 UI 回归、截图与成品 SHA-256 |
| `output/experience-browser/report.json` | 界面与分页 5 组检查、常规窗口几何数据及同目录截图 |
| `output/experience-layout-browser/report.json` | 大字号／三语／小屏 5 组检查、21 组合几何数据及同目录截图 |
| `output/experience-response-browser/report.json` | 连锁／响应／音频 7 组检查、每首音频时长及同目录截图 |
| `output/experience-final-*.log` | 最后一轮命令输出 |
| `output/build-report.json` | 正式构建路径、源文件列表、卡图／音轨／卡框统计 |

这些报告和截图是本机生成物，已被 `.gitignore` 排除；Git 中保留测试代码、复现步骤和本节结果。该验证没有覆盖每张卡的全部互相作用、官方全部裁定或所有真实手机／浏览器。

### assets 审核与构建输入

| 资源 | 决定 | 原因 |
| --- | --- | --- |
| `assets/duel/card-back.jpg` | 提交，1 张，48,777 字节 | 构建脚本直接读取的卡背 |
| `assets/duel/frames/*.webp` | 提交，11 张，共 140,532 字节 | 各类型卡框的正式构建输入 |
| `assets/duel/art/*.webp` | 提交，245 张，共 8,170,484 字节，约 7.79 MiB | 预设和连锁／卡框预览所需的压缩卡图，保证重新构建后的离线体验 |
| 素材 README、卡图和音乐 manifest、采集脚本 | 提交 | 保留来源、哈希和恢复方式 |
| `assets/duel/art/source/`、`assets/duel/frames/source/`、试验封面 | 忽略，保留本机文件 | 原始下载及试验资料 |
| `assets/audio/*.mp3`、`*.m4a` 等源音频 | 忽略，保留本机文件 | 可按清单取得；本次正式 HTML 已内嵌全部十首，不额外重复提交源音频 |
| `assets/official-archive/`、`assets/build-art/`、`output/`、`node_modules/` | 继续忽略 | 批量档案、构建缓存、验证输出与依赖 |

审核中逐张对照卡图清单和 SHA-256，并按内容哈希检查重复。245 张 WebP 没有重复文件，最大单张 69,934 字节；全部 257 张提交图片都已用 Sharp 完整解码到像素。“连接栗子球”用于 Link 卡框预览，已补入采集器与 manifest，避免再次采集时遗漏。

根目录 `index.html` 沿用现有仓库的单文件发布入口，随本次修改提交。正式构建含 **245 张卡图、1 张卡背、11 张卡框、10 首 BGM**，约 **44.38 MiB**；其余未内嵌卡图依用户设置在线补充。源音频缺失时重新构建会改用清单中的在线 URL，需要完整离线 BGM 时先运行 `audio:collect`。

提交前再次执行了两种构建。正式成品共 **46,536,996 字节**，SHA-256 为 `f7dfa07aed9a3de398ec17571941d940a8cb4ae784818112c46e250ab45d73f5`，与已通过上述两套原有浏览器回归的成品完全一致。

另外用 `git checkout-index` 将暂存文件单独导出到 `output/submission-staged-20260912-181233/`，在不带下载原件和本地音频缓存的情况下成功重新构建；结果仍含 245 张卡图、11 张卡框和卡背，10 首音乐使用来源 URL。对该构建做了离线浏览器启动与继续对局检查，未出现脚本错误，报告为 `output/submission-source-smoke.json`。这项检查验证提交资源足够，避免构建暗中依赖未提交文件；本机依赖仍复用当前安装的 Node 工具链。

---

以下保留 2026-09-09 的原有交接内容，便于追溯当时的数据来源、架构和验证范围。

日期：2026-09-09。当前操作指南见 [MANUAL.md](MANUAL.md)。

## 用户目标与不可丢失的约定

1. 缺图必须仍能正常运行。
2. 图片由用户以后手动采集；维护好增量脚本即可。图片优先级低于所有内容开发。
3. 先落实 1999、2000、2001 三年卡片和仪式召唤。
4. 最终交付一个编译后的 `index.html`，保留源码、原始数据、图片、测试报告和历史中间文件。
5. 后续按年推进到 2026 截至当时实际发行的卡片。采集完成、效果实现和官方年度核验是不同状态。
6. 默认中文，支持中英日即时切换；卡片详情保留原图链接并增加对应百科。
7. 保留可选的「在线原版卡图」，尤其供无图版本使用。用户后续明确要求恢复此功能，覆盖 V4 曾采用的“运行时绝不联网取图”限制。
8. 手动采图必须显示实时进度，继续支持增量、断点后续取及日志归档。

## 已完成的工作

- 接入用户已下载的真实三年快照：1999 / 623，2000 / 337，2001 / 242，合计 1,202 个供应方身份。
- 保留 39 个原卡身份，新增 1,163 张，使可组卡总数为 1,367。新增卡 ID 采用 `early-<providerId>`。
- 导入器是离线、可重复执行的；记录输入哈希、原始英文说明、首次 OCG 日期候选、图片键及来源。
- 三年条目已全部连接到本作基础规则、通用效果模板或专用处理器。`pending` 列表为空。这里的完成口径是“本作实现已登记”，不是每张卡通过官方裁定认证。
- 16 对仪式怪兽 / 仪式魔法已逐对执行测试。支持主卡组、手牌 / 场上解放、固定等级要求、禁止多余解放、区域校验、表示选择、AI、中途保存及正规召唤标记。
- 补充反转与战斗后触发、准备阶段、临时控制权、怪兽装备、代用融合素材、通常召唤无效时点、陷阱怪兽、卡通及灵魂怪兽等早期机制。
- 交织绵羊新增仪式分支；修正黑魔术少女漏计混沌之黑魔术师的问题。
- 新增「仪式 · 混沌的降临」「融合 · 初代的羁绊」两套 40 张预设，预设总数 13。原有 DM、HERO、黑羽、废品、希望皇、机壳、艾克佐迪亚、电子龙、水晶机巧、珠泪和 Link 内容保留。
- 图鉴和组卡工坊支持单年 / 累计年份筛选；图鉴 24 张一页、工坊 30 张一页；大候选宣言界面采用可搜索文字列表。
- 原版卡图读取本地档案并在构建时内嵌，使用 Blob URL 复用已解码资源。V4.1 恢复可选在线补图；本地优先、失败回退，缺图不影响规则。
- 新增 `collect-game-art.mjs`，维护 `collect-years.mjs`；同一图片档案按哈希增量复用。`--plan` 不联网，`--offline` 不联网，`--no-images` 成功时返回 0。
- 添加 Node / 工具版本声明和锁文件，完整操作文档、规则 / 采集测试、年度混合模拟及浏览器验证脚本。
- 为全部 1,367 张可组卡卡片配齐中、英、日名称、说明和灵摆分段，保存三份原始 CDB、来源提交和哈希。语言切换不会修改卡片规则身份。
- 全部 1,367 张卡具有实际 YGOPRODeck 百科地址。详情中的百科与原图链接并列；卡图关闭或缺失时仍可看百科。
- 主要界面、具体效果按钮、卡片属性 / 种族、玩法指南和常见记录支持三语；切换保留对局、素材、表示、区域、连锁选择、筛选及组卡草稿。自建卡组名称不会自动翻译。
- 两个采集器默认输出进度和每 5 秒等待状态，实时追加 `progress.log`。`--quiet` 保留最终 JSON 和归档日志；`--plan` 不联网、不改卡图索引。

## 数据与成果在哪里

```text
data/yearly/1999/cards.json
data/yearly/2000/cards.json
data/yearly/2001/cards.json
data/yearly/manifest-1999-2001.json
output/yearly-collection/run-2026-09-09T02-33-23-930Z/raw/catalog.json
```

该原始目录 SHA-256：

```text
e536053bf7d3d22eca0c91e4370e30c76881c855888ff6afb54f507b73a5769a
```

开始本轮开发时，用户本地已经有 2,404 个图片档案条目（cropped / full 各一套）。本轮没有为了补齐图片而等待采集；读取已有文件用于最终构建。后续状态以档案索引和构建报告为准。

开发前备份：`output/v3-content-baseline-20260909-125221/`。年度效果原文审计分组：`output/early-card-audit/`。所有构建和验证报告继续按时间归档。

V4.1 开发前另保留 `output/v4.0-before-localization-20260909-184922/`。本次只使用用户已经下载的图片，未为开发批量采图；交付构建可嵌入 1,367 张，后续以 `output/build-report.json` 实际数量为准。

最终成品是根目录 `index.html`；无图验证成品是 `output/no-art/index.html`。不要把无图输出路径误当成根目录正式构建。

## 卡片数据从哪里来：来源、字段和处理过程

当前卡池由**原有手工维护卡池 + YGOPRODeck 年度资料 + 社区三语文本库 + 本项目效果代码**共同组成。卡名和效果说明是资料，实际发动条件、代价与结算是程序；两者必须分别追溯。以下说明依据当前导入器和同步脚本，不能将整个卡池笼统标为“来自同一个数据库”。

### 1. 年度卡片的基础资料：YGOPRODeck API v7

1999—2001 年的卡片目录和基础字段来自第三方 **YGOPRODeck** 的公开 [cardinfo API](https://db.ygoprodeck.com/api/v7/cardinfo.php)，不是 KONAMI 官方数据接口，也不是从卡图上识别出的文字。当前批次的实际请求地址保存在 [采集范围配置](data/yearly/scope-1999-2001.json) 的 `catalogUrl`：

```text
https://db.ygoprodeck.com/api/v7/cardinfo.php?misc=yes&startdate=01%2F01%2F1999&enddate=12%2F31%2F2001&dateregion=ocg
```

`misc=yes` 请求附加资料，`dateregion=ocg` 指定 OCG 日期范围。采集后仍逐条检查 `misc_info[].ocg_date`，不以 TCG 日期替代。原始响应保存在上节列出的 `raw/catalog.json`，其路径和 SHA-256 由 [年度批次清单](data/yearly/manifest-1999-2001.json) 记录。

[年度采集器](scripts/collect-years.mjs) 调用 [规范化模块](scripts/lib/yearly-catalog.mjs)，将原始响应整理为 `data/yearly/<年>/cards.json`；[年度导入器](scripts/import-early-cards.mjs) 再离线生成 `src/early-cards.js`。年度文件保留 `sourceSnapshot`，各卡保留 `sourceRecordIndex`、`providerId`、原始英文说明和日期核验状态，以便回到原始记录。

### 2. 当前显示的中英日卡名与说明：三语文本库

中文、日文的卡名和效果说明来自社区项目 [mycard/ygopro-database](https://github.com/mycard/ygopro-database)，由 [三语同步器](scripts/sync-locales.mjs) 从 SQLite CDB 的 **`texts` 表读取 `id`、`name`、`desc`**。当前固定提交是 `ac5d8fb80465e74b33f3b4a294e3b22c02d743f2`，对应原始下载地址为：

- [简体中文 cards.cdb](https://raw.githubusercontent.com/mycard/ygopro-database/ac5d8fb80465e74b33f3b4a294e3b22c02d743f2/locales/zh-CN/cards.cdb)
- [英文 cards.cdb](https://raw.githubusercontent.com/mycard/ygopro-database/ac5d8fb80465e74b33f3b4a294e3b22c02d743f2/locales/en-US/cards.cdb)
- [日文 cards.cdb](https://raw.githubusercontent.com/mycard/ygopro-database/ac5d8fb80465e74b33f3b4a294e3b22c02d743f2/locales/ja-JP/cards.cdb)

本地原件分别是 `data/locales/sources/<commit>/zh-CN.cdb`、`en-US.cdb`、`ja-JP.cdb`；每份文件的大小、SHA-256、取得时间和来源提交见 [文本来源清单](data/locales/source-manifest.json)。这是社区整理的文本，不应标为本项目直接从 KONAMI 抓取的官方中文翻译。

**英文显示还有明确的优先顺序**：卡名采用卡片定义的 `officialName`；说明优先使用已保存 YGOPRODeck 响应的 `desc`，其次为年度导入保留的 `originalDescription`，两者都没有时才使用英文 CDB 的 `texts.desc`。因此不能简单描述为“三种语言的所有字段都直接取自 CDB”。中文和日文则使用各自 CDB 的卡名与说明。

灵摆卡的 `pendulumDescription` 和怪兽 `description` 从来源说明中的灵摆 / 怪兽段落标记拆分。提取结果保存在 `data/locales/cards.json` 和生成文件 `src/card-locales.js`，运行时由 `src/i18n.js` 的显示代理提供。`locales:sync` 从保留快照离线生成；`locales:update` 才主动更新上游快照。切换语言不需要在线翻译，也不依赖卡图。

### 3. 逐字段来源表

下表的“年度新增卡”指本批真正新增的 1,163 张。原有 204 张及其中与年度快照重合的 39 张，另见下一节。

| 数据 / 字段 | 实际来源与处理 | 追溯位置 |
| --- | --- | --- |
| 身份 `providerId`、`cardUid`、内部 `id` | 年度资料取 API 的 `id`；规范化为 `providerId`、`ygoprodeck:<id>`，新卡内部 ID 为 `early-<id>`。重合卡复用旧内部 ID | 原始 `catalog.json` → 年度 JSON → 导入器 |
| 英文识别名 `officialName` | 年度资料取 API 的 `name`；原有卡来自手工卡片定义，个别有代码修订。用于匹配，不随界面语言改变 | 年度 JSON 的 `name`；原有卡片模块 |
| 界面卡名 `name` | 当前中 / 日文采用对应 CDB 的 `texts.name`，英文采用 `officialName`。年度生成模块里的中文别名或英文回退名不等于当前界面的最终显示名 | `data/locales/cards.json`、`src/card-locales.js`、`src/i18n.js` |
| 效果说明、通常怪兽描述 | API `desc` 保存为年度 JSON 的 `description`、导入卡的 `originalDescription`；当前显示按上一节的三语来源与优先级读取。灵摆说明另拆段 | 原始响应、三语 CDB、`scripts/sync-locales.mjs` |
| 种族 `race` | 年度新增卡取 API `race`，导入器映射为规范中文值，例如 `Zombie` → `不死族`、`Warrior` → `战士族`。英文 / 日文种族标签由本项目界面词典翻译 | `scripts/import-early-cards.mjs` 的 `races`；`src/i18n-data.js` |
| 属性 `attribute` | 年度新增卡取 API `attribute`，例如 `DARK` → `暗`、`LIGHT` → `光`；魔法 / 陷阱使用本项目的显示标记。英日文标签由界面词典转换 | 导入器的 `attributes`；界面词典 |
| 攻击力 `atk`、守备力 `def`、等级 `level` | 年度新增卡取 API 同名字段并保留数值；原有卡使用手工定义。对局中的增减数值由引擎和效果代码计算 | 年度 JSON、卡片模块、`advanced-engine.js` / 年度扩展 |
| 卡片类型 `type`、魔法 / 陷阱子类 | API `type` 先保存为 `providerType`，再映射为引擎的怪兽、仪式、融合、魔法、陷阱类型。对魔法 / 陷阱，API 的 `race` 表示 `Normal`、`Quick-Play`、`Continuous` 等子类，导入为 `spellKind` / `trapKind`，不是怪兽种族 | 规范化模块、年度导入器 |
| 阶级、Link 值 / 箭头、灵摆刻度 | 现有现代卡的 `rank`、`linkRating`、`arrows`、`scale` 来自手工扩展定义。规范化器会保存 API 的 `linkval`、`linkmarkers`、`scale`，但当前年度导入器仍限于早期类型，不能据此声称未来现代卡已自动完整导入 | `src/expansion-cards.js`、`src/link-cards.js`、规范化模块 |
| 反转 / 灵魂 / 卡通标记、召唤限制、融合素材、仪式对象 | 年度导入器根据 `providerType` 与英文说明做有限映射 / 提取；复杂条件由本项目的卡片定义和效果模块补足。不是把任意卡文自动编译为全部规则 | 年度导入器、`effects-early*.js`、引擎扩展 |
| 年度 `releaseYear`、`firstOCGDate` | 来自 API `misc_info[].ocg_date`；按该日期分年。保留供应方候选标记，尚未完成官方年度产品交叉核验 | 原始响应、年度 JSON、覆盖报告 |
| 系列 `family`、AI 参数、预设构筑 | 本项目手工配置；年度新卡默认归入 `early`。这些玩法标签不等于 API 原始 `archetype`，也不等于官方卡包分类 | 卡片 / 预设模块、效果模块 |
| 卡包参考资料 | API `card_sets` 保留为年度记录的 `providerSetReferences`，可能包含跨年再版和 TCG 资料，不可直接当成该年 OCG 首发产品全集 | 原始响应、年度 JSON |
| 百科地址、图片身份 | 百科取实际元数据的 `ygoprodeck_url`；图片取 `card_images` 的身份与地址，另有已缓存的英文名匹配。与卡片文字、效果实现分别维护 | 原始响应、`data/providers/game-art-identities.json`、三语记录、图片档案索引 |
| `effect`、`earlyRules`、`implementationStatus`、`implementationNote` | 本项目的处理器键、规则参数和实现登记；来自导入初值及后续手工效果注册，不是供应方提供的可执行效果或完成认证 | `src/advanced-effects.js`、`src/effects-*.js`、效果覆盖报告 |

CDB 中虽然也有 `datas` 表，当前同步脚本只在核对跨来源编号别名时用其中的攻守、等级、别名等值。**它没有用 CDB 的整套数值或种族字段覆盖游戏规则数据**；种族和属性的英日文标签同样来自本项目词典，而不是逐卡再查 CDB。

### 4. 原有卡池和重合卡的处理

年度导入之前的 **204 张可组卡卡片**由开发过程手工编写在 [基础卡池](src/cards.js)、[扩展卡池](src/expansion-cards.js)、[珠泪 / Link 卡池](src/link-cards.js) 中，包括基础数值、原始中文简述、名称、系列与素材条件。现存这些定义没有为每个字段保存逐卡网页引用；不能将它们描述为这次年度 API 的自动导入结果，也不能仅凭后来补齐三语文本就称其基础字段已逐卡官方核验。

与三年快照重合的 **39 张卡**保留原内部 ID 和原有基础定义 / 效果实现，年度导入主要补入供应方身份、英文原文、日期、图片键等信息。其余 **1,163 张**从年度资料生成，所以合计为 `204 + 1,163 = 1,367`。这些卡随后都接入三语显示层；“显示了新来源的卡文”不意味着数值和效果代码也被自动替换。

### 5. 实际卡片效果代码的来源

实际效果由本项目根据卡片说明手工拆解为发动条件、目标选择、代价、结算、持续适用、诱发时点和 AI 行为，编写在 `src/advanced-effects.js`、`src/effects-*.js` 及引擎扩展中。本项目没有通过上述文本同步器下载 / 执行 YGOPro 的 Lua 卡片脚本，也没有在游戏运行时让模型解释卡文。

导入器仅做有限的结构化提取，完整行为仍须实际处理器与测试支撑。后来者改卡文时，应分别核对**显示文本、基础定义、效果代码、已有适配说明和测试**；仅更新 CDB 或 JSON 不会自动改变对局行为。

### 6. 一张卡的追溯示例

以白骨为例，供应方 ID 为 `32274490`，内部 ID 为 `early-32274490`：

1. 在保留的原始 `catalog.json` 中查 `id: 32274490`，能看到 `name: Skull Servant`、`type: Normal Monster`、`race: Zombie`、`attribute: DARK`、`atk: 300`、`def: 200`、`level: 1` 及英文 `desc`。
2. 在 `data/yearly/1999/cards.json` 中查 `providerId: 32274490`，能追到来源快照和 `sourceRecordIndex`。
3. 年度导入把它转换为 `src/early-cards.js` 中的不死族、暗属性、1 星、300 / 200 的通常怪兽；通常怪兽按基础规则运行，无需发动效果处理器。
4. `data/locales/cards.json` 中对应记录提供 `白骨` / `Skull Servant` / `ワイト` 三语名称与各自说明，并保存 [该卡百科页面](https://ygoprodeck.com/card/skull-servant-2742)。

追溯其他卡也采用同一顺序。遇到原有卡或编号别名时，额外检查手工卡片模块和语言覆盖报告中的别名证据，不能仅靠中文卡名做猜测匹配。


## 架构与容易踩到的点

### 数据与注册顺序

浏览器构建顺序由 `scripts/build.mjs` 显式维护。基础卡片 → 扩展 / Link → 生成年度卡片 → 预设 → 规则引擎 / 年度扩展 → 效果注册 → 生成三语资料 / 界面词典 / 显示代理 → 图片层 / UI。

CommonJS 入口为 `src/advanced-engine.js`。它加载年度扩展和效果注册，避免首次分析年度预设时仍把生成数据的 pending 初值视为最终状态。

`src/early-cards.js` 是生成物。不要将长期修改只写进它。效果代码在 `effects-early.js`、`effects-early-spells.js`、`effects-early-monsters.js`、`effects-early-traps.js`、`effects-early-complex.js`、`effects-early-advanced.js`。

### 规则与状态

`act()` 是原子操作，失败要恢复随机种子和整份状态。新字段也必须参与回滚。保存格式继续为 `state.version === 3`；项目版本 4.1.0 不等于存档版本 4。

不要把解放代价、规则送墓、连接 / 同调素材和效果送墓混用。珠泪、三眼怪、魔女及送墓反应依赖这些区别。

仪式解放在仪式魔法结算时处理，不是发动代价；等级要求来自仪式魔法，不能直接拿被降星后的仪式怪兽等级代替。

召唤无效窗口中，怪兽暂用 `summonPending` 标记；成功后才触发登场效果。被无效的卡发出 `from: summon-pending` 事件，不能虚构“从场上送墓”。

怪兽被吸收为装备使用 `monsterEquip`，不能先假送墓再装备。墓地效果的发动者应采用卡片到达墓地后的控制者 / 所有者，而不是机械沿用离场前的控制方。

新增多步效果请使用 `queue()`、`queueChoice()` 和命名 `E.op()`。存档里不能放函数、DOM 节点、Promise 或闭包。新的参数和选择需能 JSON 往返。

### 图片

当前运行的是 **`artwork-hybrid.js`**。`artwork.js` 和 `artwork-local.js` 保留作历史参考，不要同时放进构建列表。

`DuelArt` 优先读内嵌图片，再按用户开关加载在线图片，最终回退到可读卡面。无图构建的 `onlineArtDefault` 是 true，常规构建是 false；旧用户偏好优先。当前卡池从 `card-locales.js` 或卡片数据取得准确图片 ID，不需逐张请求元数据接口；以后未知身份才懒查询英文名，最多并行两条。关闭开关会中止未完成元数据请求并忽略迟到结果。

偏好键 `duel-local-art-enabled-v4` 控制总显示，`duel-sanctuary-online-art-v2` 控制在线补图，旧身份缓存键 `duel-sanctuary-artwork-v2` 继续兼容。线上图片不会自动写进项目目录，供以后构建仍须使用采集器。

两个采集器使用同一个 `assets/official-archive/index.json`。原件按内容哈希保存；`gameIds` / `cardNames` / `imageKeys` 用于匹配；完整卡面不直接重复内嵌到每个 DOM 元素。

原始文件、派生文件与构建缓存含义不同。手动批量删 `output/` 或 `assets/` 会丢失用户明确要求保留的中间资料。

### 三语资料和显示层

来源提交为 `mycard/ygopro-database@ac5d8fb80465e74b33f3b4a294e3b22c02d743f2`；之后以 `data/locales/source-manifest.json` 为准。原始 `zh-CN.cdb`、`en-US.cdb`、`ja-JP.cdb` 保存在 `data/locales/sources/<commit>/`，同步工具通过 Node 自带 SQLite 只读提取。

`src/card-locales.js` 是生成物。长期文字修订应在来源修订 / 同步器中保留依据，不要只改生成文件。百科 URL 来自已保存的元数据，禁止用猜测的英文 slug 冒充确切地址。

Barrel Dragon 存在跨来源编号别名：`providerId` / 图片 ID `81480461` 对应文本 `textId: 81480460`。已通过唯一英文名、攻击力、守备力和等级核对。别名在覆盖报告中保留；不能因此改写游戏或图片身份。

`DuelData.CARDS` 及 `DuelEffects` 是规则层的规范数据；**不要把已翻译的种族、属性或显示名写回规则层**。界面使用 `DuelI18n.cards` / `card()` 显示代理。卡组与素材操作仍按稳定 ID，搜索通过 `searchText()`（含效果）或 `searchName()`（仅名称 / 编号）匹配三语。

`i18n-data.js` 维护界面词典和动态模板，`i18n-effects.js` 维护具体效果按钮，`i18n-help.js` 维护指南。旧界面通过 DOM 文本翻译兼容，`data-action`、字段值和卡片编号不被翻译；新文案应同时提供三语。用户输入用 `data-user-content` 标记，卡片原文直接从本地三语记录显示。

语言偏好键是 `duel-sanctuary-language-v1`，值为 `zh-CN`、`en`、`ja`。切换触发 `duel-language-change`；`game-v2.js` 保存并恢复弹窗上下文、当前选择和滚动位置。新增弹窗类型时记得接入这条重绘路径，尤其不能清空已选素材或重开决斗。

## 验证入口

```powershell
npm.cmd ci
npm.cmd run cards:import
npm.cmd run locales:sync
npm.cmd run cards:coverage
npm.cmd test
npm.cmd run test:v4-simulation
npm.cmd run build:no-art
npm.cmd run build
npm.cmd run test:browser
npm.cmd run report
```

最新结果分别在：

- `output/v4-checks-report.json` 与对应 `results.tap`。
- `output/v4-simulation-report.json`；失败时保存对局快照及最近操作。
- `output/v4-browser-report.json`；有成品哈希、截图、脚本错误和 HTTP 请求计数。
- `output/v4.1-browser-report.json`；三语、选择保留、百科、模拟在线补图、内嵌优先和手机布局。
- `output/localization/card-coverage.json`；当前三语覆盖、缺失项和编号别名。
- `output/build-report.json`；有源文件列表、体积、内嵌图片数和图片告警。
- `output/v4-delivery-report.json`、`output/v4-QA.md`；聚合当前成品的哈希和已通过验证，生成时检查浏览器报告与成品一致。

旧 `tests/v3-browser.cjs` 保留了旧版界面断言，不是当前默认验证。`test:browser` 依次执行 `v4-browser.cjs` 和 `v4.1-browser.cjs`。前者显式关闭在线开关并验证零 HTTP 请求，后者以模拟图片响应测试可选网络路径，不能把前者的零请求结论泛化为“游戏没有在线功能”。

`npm test` 包括全部旧规则、资产采集、实时进度及三语完整性检查。`tests/i18n-audit.cjs` 和 `mobile-i18n-audit.cjs` 另保存界面截图和布局诊断；这些截图不能代替功能断言。

## 后续必须继续做的事

### 1. 官方年度目录核验

目前只使用供应方 `firstOCGDate` 候选。尚未独立核对 1999—2001 年全部卡包、预组、附卡、赠卡等产品，也没有证明不存在遗漏或错年。不能把“1,202 条全部导入”改写为“已证明收录三年官方全集”。

`Amphibious Bugroth` 的两个指定素材 `Ground Attacker Bugroth`、`Defender of the Sea` 不在本批快照中；导入保留名称约束，没有补造数据。`Temple of the Kings` 的 `Mystical Beast of Serket` 支线要在相关卡数据补入后再次验证。

本轮另行查询了 Bugroth 的公开元数据，原始响应保留在 `output/yearly-plan-research/20260909/bugroth.json`。供应方将 `Ground Attacker Bugroth` 记为 `2002-08-22` 首次 OCG，落在当前切片之外；没有擅自将它改年加入。另一个素材名称未能通过精确查询匹配，后续应核对名称及正式发行资料。

### 2. 逐卡裁定与组合审计

通用规则和关键流程已有自动测试，但远不是所有卡的全部互相作用。优先继续检查：

- 同时送墓 / 同时特殊召唤的完整时点、选发“时”与“场合”的差异。
- 持续无效效果之间的适用顺序，尤其 Jinzo、Royal Decree、Skill Drain、Imperial Order。
- 守备表示直接攻击、强制攻击、战斗卷回、多次战斗阶段、临时控制权与完整伤害步骤的组合。
- 陷阱怪兽 / 魔术礼帽临时怪兽与所有融合、连接、仪式素材规则的交互。
- 各卡特殊召唤条件中的“必须先”与“只能”的精细区别、隐藏区域的合法性与信息披露。
- 卡通、灵魂与旧卡勘误后的细节；目前使用当前供应方文本，不模拟历史规则版本。

有两项明确的本作适配：**Spirit Elimination** 预先指定替代除外顺序；**Bait Doll** 的可发动陷阱走后续连锁。已写在卡片实现说明和手册里。后续若改为完整官方处理，应同步更新测试及这些说明。

“implemented”是运行代码的登记状态，不是测试通过状态，更不是正式裁定认证。需要添加独立的逐卡裁定审计维度时，请新增字段，不要复用年度、图片或实现状态。

### 3. 从 2002 年开始的新批次

先用 `cards:year` + `collect:years --no-images` 准备下一年资料，再按小批次实现效果。当前导入器、筛选选项和覆盖报告的接入范围是 1999—2001；增加年份时同步扩展这些入口。

后续年份的供应方类型还可能包括同调、超量、灵摆、连接、调整等。虽然引擎已有这些召唤，**年度导入器的类型映射仍需随下一批扩展**，不能把所有新类型直接当作普通怪兽导入。

2026 批次应按执行当日截断实际发行日期；不要把仅已公告的未来产品纳入“至今”。目前 `prepare-year` 生成整年采集范围，届时要补截止日期核验。

### 4. 持续维护文案和工具

新增卡后运行 `locales:sync`，尽量从已保留快照离线提取三语；快照未收录的新卡再手动运行 `locales:update`。新增的特殊效果按钮、选择提示和实现适配说明也要补三语，不能只补卡名。社区库的译名与各地发行文本可能不同，保留来源及原文追溯，不把译名猜测当作官方资料。卡片内容和效果优先，图片最后由用户自行增量处理。

以后每批完成时，更新本交接文档、手册、覆盖报告和测试记录。没有被授权发布、部署或提交 Git，本轮也没有替用户执行这些动作。
